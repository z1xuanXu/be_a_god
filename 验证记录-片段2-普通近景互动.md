# 验证片段 2：普通近景互动

- 日期：2026-08-12
- 当前世界/分支：无正式世界；本次为设计干跑验证
- 玩家输入：

> 点击河谷城码头上的流浪祭司棋子，问他为什么要把断裂的铜环丢进河里。

## 本轮允许读取

- 当前分支 `ACTIVE.md` 与 `SAVE.md`。
- 当前场景摘要：河谷城码头。
- 目标角色卡：流浪祭司当前状态、知识、目标、关系、秘密边界。
- 目标角色实体卡明确指向的少量相关事件摘要。
- 玩家点击棋子产生的动作请求。

不允许读取：

- 整个世界设定全文。
- 全部章节正文。
- 远方地区历史。
- 无关人物卡。
- 任何平级分支内容。

## 设计干跑输出

### 前端点击应转成动作请求

前端点击棋子后，不直接写正式剧情，而是生成一条动作请求：

```yaml
request_id: ACT-000001
branch_id: BR-MAIN
world_time: <当前世界时间>
map_layer: scene
location_id: LOC-RIVER-CITY-DOCK
target_id: CHAR-WANDERING-PRIEST
action_type: ask
player_text: 为什么要把断裂的铜环丢进河里？
source: dashboard-piece-click
```

该请求应进入当前分支的 `runtime/action-requests.jsonl`，或在无本地服务时作为可复制的自然语言命令。

### 小型交互包应包含

- 当前场景、时间、天气和局部环境。
- 目标角色当前公开状态。
- 目标角色本轮可用知识，不包含其不知道的秘密。
- 与“铜环”“河”“誓约”“码头”直接相关的事件摘要。
- 玩家原话。
- 内容边界和当前叙事视角。
- 本轮禁止读取项。
- 如需补读旧事，必须列出具体事件 ID 或原文指针。

### 互动结果应分三档落盘

- 无长期影响：只追加到当前章节正文，不创建重要事件。
- 有轻微状态变化：追加章节正文，并写入人物状态差异。
- 触发长期后果：创建事件节点，更新状态、队列、客观简史、时间线和前端数据。

## 通过情况

结果：部分通过。

已通过：

- 蓝图已明确普通互动默认走快速路径。
- 蓝图已定义小型交互包的大致内容。
- 蓝图已规定只有旧承诺、秘密、事实冲突或不可逆后果才补读历史。
- 蓝图已把 `make_interaction_packet.py` 纳入脚本结构。

发现的问题：

1. 没有明确交互包的临时保存位置，后续实现可能把它误当成正式事实。
2. 没有明确前端点击棋子后动作请求记录在哪里。
3. 没有明确“AI 发现需要补读旧事”时如何申请补读，容易退化成读取大量历史。
4. 没有独立的互动结算脚本名，可能导致剧情生成和机械写回混在一起。

## 建议修订

- 在每个分支下增加 `runtime/action-requests.jsonl` 与 `runtime/interaction-packets/`。
- 明确 `runtime/` 是临时运行资料，不是事实来源。
- 为小型交互包增加固定字段：`allowed_sources`、`forbidden_sources`、`source_budget`、`followup_source_request`。
- 新增 `settle_interaction.py`，负责把互动结果机械写回章节、事件、状态、编年史和前端数据。

## 是否继续片段 3

可以。片段 2 的问题是实现落点缺口，不影响继续验证，但建议先补回蓝图。
