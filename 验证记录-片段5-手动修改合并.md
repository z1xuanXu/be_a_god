# 验证记录：片段 5 - 玩家手动修改 Markdown 后合并

> 验证日期：2026-08-12  
> 对应蓝图版本：v0.19  
> 验证性质：设计干跑，不创建真实世界目录，不执行正式游戏代码。

---

## 1. 玩家行为样例

玩家在 Codex 对话外直接打开当前枝丫的 Markdown 文件，做了两类修改：

1. 在人物卡中，把祭司的公开身份从“流浪祭司”改为“被放逐的河神庙司铎”。
2. 在一个事件节点中，误把事件 ID 从 `EVT-0142` 改成了 `EVT-0412`。

玩家随后回到 Codex，要求继续游戏。

---

## 2. 期望系统行为

继续游戏前，系统不应直接读取修改后的文件并把它当成正史。正确流程应是：

1. 先运行 `detect_manual_edits.py`。
2. 通过 `system/file-manifest.json`、最近检查点和当前文件内容对比，识别发生变化的文件和字段。
3. 将“公开身份修改”标记为中风险事实修改。
4. 将“事件 ID 修改”标记为高风险结构修改。
5. 在当前枝丫生成 `runtime/manual-edit-reports/<report_id>/report.md` 和 `merge-plan.json`。
6. Codex 只向玩家展示简短影响范围，不自动合并。
7. 玩家可以接受身份修改，拒绝事件 ID 修改。
8. `apply_manual_edits.py` 按玩家确认创建检查点，执行合并，重建索引、时间线、前端数据和验证报告。
9. 合并成功后写入 `system/manual-edit-ledger.jsonl`，并刷新 `system/file-manifest.json`。

---

## 3. 蓝图现状检查

已具备的设计：

- 已明确“玩家手动修改 Markdown 后，系统不自动接受为正史”。
- 已有 `detect_manual_edits.py` 与 `apply_manual_edits.py` 两个脚本职责。
- 已有低风险、中风险、高风险三类手动改动分类。
- 已规定派生文件不能反向覆盖核心事实。
- 已规定改变已发生历史时默认建议创建新枝丫。

发现的缺口：

- 缺少明确的文件指纹基线文件。仅说“上次记录的文件指纹”不够，脚本后续难以知道以哪个文件为准。
- 缺少手动编辑报告和合并计划的落盘位置。没有报告目录，玩家确认、拒绝和阻塞项无法稳定复查。
- 缺少“事实性手改被接受后如何进入事件树”的规则。只更新人物卡或 `SAVE.md` 会让编年史、时间线和事件指针失去来源。
- 缺少阻塞项清单。高风险项如果不能自动证明自洽，应明确进入 `blocked_changes`，不能部分静默合并。

---

## 4. 验证结论

结果：部分通过。

核心原则正确，但 v0.18 的工程落点还不够。片段 5 暴露出手动编辑合并需要四个明确对象：

1. `system/file-manifest.json`：记录上次验证后的核心文件指纹、事实来源和派生文件清单。
2. `runtime/manual-edit-reports/<report_id>/report.md`：给玩家看的影响范围报告。
3. `runtime/manual-edit-reports/<report_id>/merge-plan.json`：给 `apply_manual_edits.py` 使用的待确认合并计划。
4. `manual-correction` / `divine-revision` 事件：记录被接受的事实性修改如何进入故事树。

---

## 5. 已补入蓝图的调整

已在 v0.19 中补入：

- 文件结构新增 `system/file-manifest.json`。
- 脚本清单新增 `build_file_manifest.py`。
- 当前枝丫 `runtime/` 明确包含手动编辑报告。
- `detect_manual_edits.py` 必须生成 `report.md` 与 `merge-plan.json`。
- `apply_manual_edits.py` 必须按报告 ID 合并，并在成功后刷新文件指纹基线。
- 事实性手动修改被接受后，必须写入 `manual-correction` 或 `divine-revision` 事件。
- 高风险冲突项必须进入 `blocked_changes`，交还玩家确认或改用新枝丫方案。

---

## 6. 后续实现提示

正式实现时，`detect_manual_edits.py` 不应只做文本 diff。至少要同时输出：

- 文件级变化。
- 实体级变化。
- 事件级变化。
- 字段级变化。
- 影响的索引、时间线、前端缓存和编年史。
- 是否触及锁定事实、随机日志、父指针或死亡/毁灭事实。

`apply_manual_edits.py` 应先创建检查点，再执行任何合并。若检查点创建失败，应停止，不允许继续写入。
