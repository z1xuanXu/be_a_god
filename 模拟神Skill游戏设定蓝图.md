# 《成为神》Codex Skill 游戏设定蓝图

> 暂定项目名：`be-a-god`  
> 文档版本：v0.83  
> 更新时间：2026-08-13  
> 当前阶段：概念设计与需求澄清  
> 文档用途：这是后续持续补全的“设计母档”，不是最终的 `SKILL.md`。

## 0. 标记说明

- **已确定**：来自当前需求，后续默认保留。
- **建议默认**：为了先做出可玩的版本而提出，可随时修改。
- **待确认**：需要玩家在后续对话中作出选择。

---

## 1. 游戏定位

### 1.1 一句话概念

玩家通过安装并调用一个 Codex Skill，成为持续运转世界中的“神”。玩家可以观察世界、干预自然、降下神谕、改变事件，也可以什么都不做，让人物、势力和环境按照自身因果继续演化。

### 1.2 核心体验

1. **世界会自己活下去**：时间、天气、人物目标、势力冲突和待触发事件会持续推进。
2. **玩家拥有真正的神权**：任何随机结果都允许被玩家覆盖，但覆盖必须留下记录，避免世界状态悄悄失真。
3. **历史可以追溯**：AI 不必重读全部剧情，只读取当前存档、相关事件节点及其原文指针。
4. **故事可以长期游玩**：换对话后仍能恢复时间、地点、人物关系、未完成事件和关键因果。
5. **操作以自然语言为主**：玩家不需要学习复杂指令；结构化命令只作为快捷方式。

### 1.3 不追求的方向

- 第一版不做大型 3D 世界或复杂实时游戏引擎。
- 不让前端面板取代叙事；地图和看板只负责观察、定位和辅助操作。
- 不依赖 AI 临时记忆保存关键事实。
- 不用一份无限增长的总剧情文件承担全部检索任务。

### 1.4 已确定的混合创世方式

新世界采用“**玩家指定与锁定 + 必要信息填空模板 + AI 补全其余世界**”的方式创建。

- 玩家只需要填写少量必要字段，也可以继续指定任何自己在意的细节。
- 每个可选字段都能切换为“玩家锁定”或“交给 AI”。
- 玩家锁定的内容逐字保留，并进入不可被普通推演覆盖的正史约束。
- 留白内容由 AI 在已锁定事实、题材边界和世界结构约束内补全。
- AI 先生成创世草案，玩家预览确认后才创建正式世界文件。
- 玩家可以只重新生成某个未锁定部分，不必推翻整个世界。

这使开局既不要求玩家写一部长篇设定集，也不会让 AI 抢走玩家真正关心的创作权。

---

## 2. 玩家身份与神权系统

### 2.1 玩家可以做什么

- **观察**：查看某个地区、人物、组织、秘密、时间段或因果链。
- **推进时间**：推进几分钟、一天、一季或若干年，也可以推进到某个事件发生为止。
- **干预自然**：改变天气、气候、灾害、资源、疾病、地貌等。
- **干预生命**：赐福、诅咒、治愈、创造、毁灭、复活或改变人物命运。
- **传达意志**：通过神谕、梦境、预兆、化身、使者或直接显现影响世界。
- **修改规则**：改变局部或全局世界法则，但必须明确作用范围和生效时间。
- **回溯历史**：查看历史、追踪因果；需要改写过去时，使用“修订历史”机制。
- **保持沉默**：让世界按既有规则和随机系统继续运行。

### 2.2 权限优先级

发生冲突时，按照以下顺序决定真实状态：

1. 玩家本轮明确下达的神谕或修改
2. 玩家锁定的世界规则与不可更改事实
3. 已确认写入历史的事件
4. 系统预定事件与人物计划
5. 随机程序产生的结果
6. AI 为叙事连贯性作出的普通推演

AI 不得用“剧情需要”否定玩家的明确神权。若一个神谕会造成世界崩坏，AI 应先说明后果，再执行；除非玩家给出的指令本身含糊到无法判断目标。

### 2.3 已确定的游戏模式

**已确定：第一版采用混合模式。** 玩家永远拥有最终修改权，同时保留“信仰/神力规则”，让常规干预产生代价，从而获得策略性。

- **纯沙盒模式**：神权无限，重点是世界构建与叙事。
- **神力经营模式**：信仰、祭祀、神格或领域决定可用能力。
- **混合模式**：正常游玩计算代价，玩家声明“无视代价”时仍可强制执行。

混合模式下，每次超越当前神力上限的行动，系统先简短告知正常代价和可能影响。玩家可以接受代价、缩小干预，也可以声明“以绝对神权执行”。强制执行不被拒绝，但会被编年史和神谕记录如实记载。

---

## 3. 核心游戏循环

每一轮遵循同一套稳定流程：

1. **载入最小上下文**：读取当前存档、世界状态、本轮涉及实体，以及必要的历史指针。
2. **理解玩家意图**：区分观察、干预、推进、回溯、修改规则或保存。
3. **判定世界变化**：先应用玩家神谕，再处理固定规则、人物行动、事件队列和随机结果。
4. **生成叙事**：以故事形式呈现本轮发生的事情，不把内部数据结构当成正文。
5. **结算长期影响**：更新人物、地点、势力、时间、天气、关系、资源和未来事件。
6. **写入记录**：追加原始剧情，创建重要事件节点，更新当前状态与索引。
7. **执行上下文检查**：达到阈值时生成阶段摘要或跨对话存档。
8. **给出下一步入口**：简短提示当前可观察或可干预的重点，不强迫玩家选择固定菜单。

### 3.1 每轮输出建议

- 第一部分：沉浸式剧情正文。
- 第二部分：仅在确有变化时显示简短的“世界变化”。
- 第三部分：仅在需要时显示存档、上下文或矛盾警告。

玩家可以随时要求“只讲故事”“显示完整结算”或“显示幕后随机结果”。

### 3.2 已确定的自动混合叙事

**已确定：默认根据玩家行为和世界推进尺度自动切换叙事视角。** 第一版使用三种核心视角：

| 视角 | 默认触发场景 | 主要表现 |
|---|---|---|
| 神明全知 | 观察世界、查看地区、比较势力、干预环境 | 从高处呈现格局、因果、隐藏风险和多个地点的变化 |
| 人物近景 | 点击角色棋子、对话、附身、观察具体场景 | 聚焦感官、动作、对白、情绪和眼前选择 |
| 编年史 | 跨月、跨年、大范围自动推进、回顾历史 | 用紧凑时间顺序概述重要事件、转折和长期结果 |

#### 自动路由规则

- 玩家停留在世界或地区地图观察时，使用神明全知视角。
- 玩家点击棋子、物体、建筑或局部环境时，切换为人物/场景近景。
- 玩家进行大跨度时间推进时，使用编年史视角跳过无意义日常。
- 编年史推进遇到剧情就绪或必须由玩家决定的事件时，立即暂停宏观叙述，切入人物近景。
- 近景事件处理完成后，回到之前的地图层级和推进方式，除非玩家选择继续跟随该角色。
- 神权造成全球性影响时，先用短暂的神明全知视角呈现影响范围，再进入受影响的关键近景或编年结算。

#### 玩家覆盖

玩家可以随时临时指定：

- “只从这个人的视角讲。”
- “不要进入近景，直接给我十年编年史。”
- “用全知视角告诉我所有势力的真实反应。”
- “隐藏幕后秘密，只展示凡人能够观察到的内容。”

单轮指定高于自动路由；长期偏好写入 `PLAYER.md`，直到玩家再次修改。

#### 视角不等于知识共享

- 玩家通过神明全知视角看到秘密，不代表任何角色知道该秘密。
- 人物近景只能让角色依据其自身知识行动，不能借用 AI 或玩家知道的信息。
- 切换叙事视角不会自动读取完整历史；仍然遵守最小交互包和按指针补读规则。
- 系统只生成一份当前需要的叙事，不把同一事件分别用三种视角重复讲述。
- 后台编年史仍按正式事件增量记录，但除非玩家要求，不在每轮完整展示。

叙事模式的选择和恢复位置可以由规则脚本登记；具体文字、镜头感、对白和情绪表达由大语言模型完成。

---

## 4. 时间、行动与世界推进

### 4.1 双层时间

- **世界时间**：故事内部的年月日、季节和时刻。
- **游戏轮次**：玩家与 Codex 的一次有效交互，用于检查存档与上下文，不等同于固定世界时长。

### 4.2 推进粒度

- 场景推进：继续到当前场景出现明确结果。
- 固定推进：如“推进三小时”“一年后”。
- 条件推进：如“直到战争爆发”“直到这个人发现真相”。
- 区域推进：只精细模拟关注区域，其他区域以摘要级别后台演化。

### 4.3 玩家自定义的混合推进

**已确定：推进方式由玩家自由组合，不把整局锁死在一种模式。**

- **逐步推进**：一次处理一个动作、一个场景或一个短时间单位。
- **固定时长推进**：推进指定小时、天、月或年。
- **条件推进**：持续到某个人抵达目的地、某场战争爆发或某条件成立。
- **事件优先推进**：跳到下一个值得玩家关注的事件，中间变化写成摘要。
- **角色跟随推进**：锁定一个或一组棋子，沿其行动持续演化。
- **区域独立推进**：焦点地区逐场景处理，背景地区按月或季度汇总。
- **授权自动推进**：在当前任务内连续运行指定轮数，或运行到停止条件；遇到玩家设定的高优先级事件时暂停。

玩家可以保存推进预设，例如：

```yaml
focus_region: scene-by-scene
active_regions: daily
background_regions: monthly
follow: [CHAR-ELIN]
auto_pause_on: [divine-sign, war, death, succession, player-decision]
stop_after: 30-days
```

同一世界的不同地区和角色可以同时采用不同精度。玩家每次推进时的临时指令高于长期预设。

“自动推进”指玩家授权后，在一次活跃任务中批量推演；第一版不假设 Codex 对话关闭后仍有常驻后台进程。

#### 批量推进运行记录

当玩家要求“推进一年”“自然运行三个月”或“运行到下一件大事”时，系统必须创建一份推进运行记录，默认写入当前分支 `runtime/advance-runs/`。该记录是审计和恢复用的临时运行资料，不是事实来源。

推进运行记录至少包含：

- `advance_run_id`、当前分支、开始世界时间和请求跨度。
- 玩家指定的停止条件和显示方式。
- 焦点层、活跃层、背景层的时间片粒度。
- 当前推进游标，以及已结算到哪个世界时间。
- 已调用的随机条目 ID 和对应事件 ID。
- 已写入的事件、客观简史条目和时间线节点。
- 如果暂停，记录暂停原因、暂停事件和恢复游标。

批量推进不能把整段未来一次性写死。`advance_world.py` 必须按时间片推进：焦点层逐场景或关键事件，活跃层按天或计划节点，背景层按月或季度摘要。每个时间片结算后更新游标；遇到停止条件立即暂停，不继续预写剩余时间。

### 4.4 精度分层

为了避免世界越大、消耗越失控，区域和人物分为三档：

- **焦点层**：当前场景，逐事件模拟。
- **活跃层**：重要人物与地区，按天或关键节点模拟。
- **背景层**：远离当前故事的区域，按周、月或季生成摘要。

当玩家观察某个背景区域时，系统先根据其上次状态和已发生的宏观事件补算，再提升为焦点层。

---

## 5. 历史存储：分支树 + 事件图 + 原文指针

### 5.1 为什么不只用一棵树

树适合记录“从哪个世界线分叉”，但不适合表达跨地区、跨人物和跨年代的多重因果。因此采用混合结构：

- **分支树**：记录主世界线、平行世界线和历史修订的父子关系。
- **事件关系图**：记录事件之间的原因、结果、参与者、地点和引用关系。
- **原文指针**：事件节点只保存精炼事实，并指向完整剧情中的具体章节或场景。
- **实体索引**：按人物、地点、势力、物品、规则和标签快速找到相关事件。

第一版不强制引入向量数据库。结构化 ID、标签和关系索引更透明、更稳定，也更容易让玩家检查。未来当剧情规模达到数千事件后，再把向量检索作为可选加速层，而不是唯一事实来源。

### 5.2 事件节点示例

```yaml
id: EVT-000142
branch: BR-MAIN
world_time: 0042-03-18T19:30
type: divine-intervention
location: LOC-RIVER-CITY
actors: [CHAR-ELIN, FACTION-ASH-CHURCH]
causes: [EVT-000137]
effects: [REL-ELIN-CHURCH, QUEUE-000021]
source: story/main/branches/BR-0001/chapters/CH-0008.md#SC-0041
tags: [神谕, 洪水, 信仰分裂]
canon: true
```

节点正文只写：发生了什么、为什么重要、改变了哪些状态、哪些问题尚未解决。完整对白和描写仍保留在原始剧情文件中。

### 5.3 检索路径

AI 需要了解过去时，按下面顺序读取：

1. `ACTIVE.md`：找到当前枝丫的 `SAVE.md`。
2. 当前枝丫 `SAVE.md`：读取当前局面、继承边界和下一步文件清单。
3. 本轮相关的人物、地点、势力或规则状态。
4. 相应实体卡中的关联事件 ID。
5. 必要的事件节点；需要父级历史时，只沿 `parent_save` 向上回溯。
6. 只有在需要核对原话、细节或伏笔时，才沿 `source` 读取当前血统链上的原始剧情片段。

这能避免为了一个人物的旧承诺而重读几十章故事。

### 5.4 快速交互读取协议

**已确定：玩家与角色、物体或环境直接交互时，默认走快速路径，先完成交互，不读取大量历史。**

交互开始前只装配一份“小型交互包”：

1. 当前场景、世界时间和局部环境。
2. 目标实体的当前状态与不可违背的事实。
3. 目标角色与本次话题直接相关的知识、目标、关系和秘密边界。
4. 玩家刚刚发出的动作或话语。
5. 至多若干个被实体卡明确指向的相关事件摘要。

默认不读取完整章节、远方历史、无关人物卡或整份世界设定。交互包由脚本根据 ID 和标签组装，设置固定大小预算；AI 直接基于交互包完成对话或互动。

交互包由 `make_interaction_packet.py` 生成，默认写入当前分支 `runtime/interaction-packets/`。它是本轮临时上下文，不是事实来源；真正的事实仍来自章节、事件节点、实体状态和玩家确认记录。

目标定位必须保守：优先读取状态卡中的精确 `- id:`，其次只允许唯一文件名前缀匹配；不允许因为某张卡片正文里提到目标 ID 就把它当作目标。显式传入的 `--target-file` 也必须位于当前世界目录内。

交互包至少包含以下控制字段：

- `packet_id`：本轮交互包 ID。
- `request_id`：来自前端或自然语言命令的动作请求 ID。
- `branch_id`、`world_time`、`location_id`、`target_id`。
- `allowed_sources`：本轮允许读取的实体、事件和场景摘要。
- `forbidden_sources`：本轮禁止读取的完整章节、无关人物、远方历史和平级分支。
- `source_budget`：本轮可装入的事件摘要和原文片段上限。
- `player_action`：玩家原话或动作。
- `knowledge_boundary`：目标角色本轮可知道和不可知道的信息。
- `content_profile_summary`：本轮需要的内容边界摘要。
- `followup_source_request`：如需补读旧事，必须在这里列出具体事件 ID、实体 ID 或原文指针。

前端点击棋子时，不直接修改正式世界，也不自动写入动作请求。点击默认只选择目标、打开轻量详情卡并显示 source pointer；当玩家继续点击“观察/对话/干预/关注/忽略”等动作按钮，或发出明确快速命令时，才写入当前分支 `runtime/action-requests/<request_id>/`，或在无本地服务时生成可复制给 Codex 的命令。动作请求进入交互包后才由 Codex 处理。

仅在以下情况按指针补读：

- 玩家明确追问旧事、原话或具体细节。
- 角色要根据过去的承诺、创伤、秘密或关系作出关键决定。
- 本轮出现事实冲突，无法仅靠当前实体卡解决。
- 交互将触发不可逆的重大结果，需要核对锁定规则。

补读必须走 `followup_source_request`，由脚本按 ID 追加极少量来源；不能让 AI 自行扩大为“读取所有相关历史”。

交互结束后，再由 `settle_interaction.py` 完成事件登记、状态差异、索引、编年史、时间线和前端数据等机械结算。普通寒暄和无长期影响的小动作只写入场景正文，不必创建重要事件节点。

### 5.5 历史修订

**已确定：玩家修改已经发生的历史时，默认创建一个以 `SAVE.md` 存在的子分支。** 它作为主故事文件树上的一个新枝丫，不覆盖父分支，也不与平级分支共享后续状态。

- 子分支保存 `parent_save`、`parent_branch_id` 和 `fork_event`，可以精确回溯上一级历史。
- 子分支只继承父分支截至 `fork_event` 的事件、人物状态、地图状态和随机位置。
- 分叉之后采用写时分离：新剧情、状态、事件、队列、地图变化和随机日志只写入当前子分支。
- 平级分支互相不可见；正常检索不得枚举、合并或借用兄弟分支内容。
- 父分支在分叉点之后继续产生的新事件，不会被已经创建的子分支继承。
- 切换世界线时更新 `ACTIVE.md`，并从目标 `SAVE.md` 重建索引和前端数据。
- 默认不支持自动合并分支；玩家若要把另一分支的内容带入当前世界，必须明确发动一次“神权导入”，并作为当前分支的新事件记录。
- 如果玩家只是改变某些角色的记忆，而没有改变客观历史，可以留在当前分支并记录知识状态变化。

因此，“改变过去”默认产生新枝丫；只有不改变客观事实的记忆修改才无需分支。

#### 创建历史修订子枝的流程

当玩家要求回到过去、阻止已发生事件或改写历史时，系统不得立刻覆盖当前分支。`create_branch.py` 先生成分支草案，默认写入当前分支 `runtime/branch-drafts/`。

分支草案至少包含：

- `branch_draft_id`、父分支、目标 `fork_event` 和 `inherit_until`。
- 玩家想改变什么，以及这是修正错录事实还是创造新世界线。
- 建议的新分支名称。
- 父分支在分叉点的 `parent_head_at_fork`。
- 将要继承的地图版本、状态版本和随机位置。
- 是否创建后立即切换 `ACTIVE.md`。

玩家确认后，`create_branch.py` 才创建正式子枝。默认行为是创建并切换到新子枝；如果玩家选择“只创建不切换”，则原 `ACTIVE.md` 保持不变，只更新 `STORY-TREE.md`。

子枝创建后必须写入第一条本枝事件，类型为 `divine-revision` 或等价的神权修订事件。该事件记录玩家如何改变分叉点附近的历史，只属于新子枝，不回写父分支。

子枝随机种子由根种子、父分支 ID、`fork_event`、新分支 ID 和父分支在分叉点的随机位置派生。子枝不得继续使用父分支分叉后的随机日志，也不得读取平级分支随机状态。

---

## 6. 文件与存档设计

### 6.1 计划中的单局目录

```text
worlds/<world-id>/
├─ setup/
│  ├─ WORLD-BRIEF.md              # 玩家填空、原话与字段锁定状态
│  ├─ world-spec.json             # 供脚本验证和初始化的结构化创世参数
│  ├─ content-profile.json        # 内容边界、呈现强度和淡化规则
│  ├─ drafts/                     # 玩家确认前的创世草案区
│  │  ├─ world-draft.md           # AI 补全后的可读草案
│  │  ├─ world-draft.json         # AI 补全后的结构化草案
│  │  └─ preview-report.md        # 玩家指定、AI 补全和程序默认的对照预览
│  └─ generation-record.jsonl     # AI 补全、局部重生成和玩家确认记录
├─ WORLD.md                       # 世界简介、主题、初始规则
├─ ACTIVE.md                      # 只保存当前枝丫及其 SAVE.md 的指针
├─ CANON.md                       # 玩家锁定的事实与世界法则
├─ PLAYER.md                      # 玩家偏好、神格、领域、展示方式
├─ story/
│  ├─ STORY-TREE.md               # 整棵故事树的可读索引，由脚本生成
│  └─ main/
│     ├─ SAVE.md                  # 主枝当前状态与跨对话继承入口
│     ├─ CURRENT.md               # 主枝热记忆
│     ├─ chapters/                # 主枝只追加剧情
│     ├─ events/                  # 主枝重要事件节点
│     ├─ state/                   # 主枝人物、势力、地图等状态变化
│     ├─ chronicle/               # 主枝编年史
│     ├─ queues/                  # 主枝待触发事件
│     ├─ random/                  # 主枝随机状态与日志
│     │  ├─ seed.json             # 初始随机种子、随机流和重放参数
│     │  └─ random-log.jsonl      # 随机调用、结果、重抽和神权覆盖记录
│     ├─ runtime/                 # 主枝临时运行资料，不作为事实来源
│     │  ├─ action-requests/      # 前端动作按钮、快捷按钮和复制命令生成的动作请求
│     │  ├─ interaction-packets/  # 即时互动的小型上下文包，可清理或重建
│     │  ├─ advance-runs/         # 批量推进的运行记录、推进游标和暂停包
│     │  ├─ branch-drafts/        # 改变过去或复制世界线前的分支草案
│     │  └─ divine-assessments/   # 神权干预代价预览，不作为事实来源
│     ├─ checkpoints/             # 主枝 SAVE.md 的历史快照
│     └─ branches/
│        ├─ BR-0001/
│        │  ├─ SAVE.md            # 子枝存档，指向父 SAVE 与分叉事件
│        │  ├─ CURRENT.md         # 子枝热记忆
│        │  ├─ chapters/          # 只保存分叉后的本枝剧情
│        │  ├─ events/            # 只保存分叉后的本枝事件
│        │  ├─ state/             # 只保存本枝状态覆盖层
│        │  ├─ chronicle/         # 本枝新增客观简史与史诗编年派生文本
│        │  ├─ queues/            # 本枝独立事件队列
│        │  ├─ random/            # 本枝独立随机流
│        │  ├─ runtime/           # 本枝临时动作请求、交互包、推进运行记录、分支草案和手改报告
│        │  ├─ checkpoints/       # 本枝存档历史
│        │  └─ branches/          # 该枝还能继续产生下一层枝丫
│        └─ BR-0002/              # 与 BR-0001 平级且完全隔离
├─ base/
│  ├─ entities/                  # 创世时确认的初始实体定义
│  └─ maps/
│     ├─ hierarchy.json           # 初始三级地图层级
│     └─ coordinates.json         # 初始地点坐标
├─ indexes/                       # 只针对当前枝丫生成的轻量索引
├─ dashboard/
│  ├─ data.json                   # 当前枝丫地图、棋子与看板数据
│  └─ timeline.json               # 当前枝丫纵向时间线数据
└─ system/
   ├─ turn-ledger.jsonl           # 轮次、文件变化、估算上下文
   ├─ file-manifest.json          # 上次验证后的核心文件指纹、事实来源和派生文件清单
   ├─ manual-edit-ledger.jsonl    # 玩家手改 Markdown 的检测、确认和合并记录
   └─ validation-report.md        # 最近一次一致性检查
```

### 6.2 Markdown 与结构化数据的分工

- 剧情、世界设定、人物卡、事件节点和存档以 Markdown 为主，方便玩家直接阅读和修改。
- 随机状态、轮次流水和前端导出使用 JSON/JSONL，方便程序稳定处理。
- 索引可以重新生成；剧情原文、神谕记录和随机结果日志不可依赖索引单独保存。
- 每个故事枝丫的 `SAVE.md` 同时承担当前状态摘要、父级指针和跨对话继承入口；其完整剧情与事件仍单独保存。

### 6.3 事实来源优先级

```text
玩家明确神谕
  > CANON.md 中的锁定规则
  > 当前枝丫及其父级血统链上、位于继承边界内的正式事件与原始剧情
  > 当前枝丫 SAVE.md 和状态覆盖层
  > 当前枝丫的阶段摘要
  > 仅针对当前枝丫生成的索引
  > 前端缓存
```

摘要和面板只能帮助读取，不能反向悄悄改写真实历史。

### 6.4 分支 `SAVE.md` 结构

每个枝丫根目录必须直接包含一个 `SAVE.md`：

```yaml
branch_id: BR-0001
branch_name: 洪水未曾发生
parent_branch_id: BR-MAIN
parent_save: ../../SAVE.md
fork_event: EVT-MAIN-000137
inherit_until: EVT-MAIN-000137
parent_head_at_fork: EVT-MAIN-000142
world_time: 0042-03-18T19:30
head_event: EVT-BR0001-000006
status: active
```

除头部指针外，`SAVE.md` 还包含当前场景、活跃实体、待触发事件、随机流位置、最近事件、下一轮读取清单和完整性校验结果。它必须足以让新对话恢复该枝丫，但不复制父分支的完整剧情。

`parent_head_at_fork` 和 `inherit_until` 会把继承范围钉死在分叉时刻。之后父分支即使继续发展，也不会改变已经存在的子分支。

读取某个分支时，`resolve_branch_view.py` 执行：

1. 从当前 `SAVE.md` 开始。
2. 沿 `parent_save` 向上直到主枝。
3. 对每一级只读取该子枝允许继承到的事件边界。
4. 从根到当前枝依次叠加状态覆盖层。
5. 生成只属于当前枝丫的临时视图、索引和前端数据。

该脚本禁止横向扫描 `branches/` 中的兄弟目录。平级分支只有在玩家主动切换或明确要求比较世界线时才可读取。

### 6.5 自动世界编年史

**已确定：游戏过程中自动记录世界编年史。** 编年史与剧情原文分开：剧情负责场景、对话和体验；编年史负责按世界时间记录已经成为事实的重要变化。每个枝丫只追加自身分叉后的编年条目；查看完整编年史时，沿父指针合成继承范围内的父级条目，绝不混入平级分支。

每个编年条目至少包含：

- 世界时间与所属纪元。
- 发生地点、参与者和事件类型。
- 一至三句客观摘要。
- 直接原因、主要结果和影响范围。
- 关联事件 ID 与剧情原文指针。
- 事件属于自然演化、随机结果还是玩家神权干预。
- 当前分支及其正史状态。

编年史采用两步生成：

1. 脚本从正式事件节点抽取时间、地点、参与者、因果和指针，按确定规则排序并追加条目骨架。
2. 只有在章节结束、纪元更替或玩家要求润色时，才让大语言模型把一批骨架整理为更自然的编年叙述。

这使每轮都能自动留痕，又不会为了写编年史重复读取大量剧情。编年史不能成为新事实来源；如果它和事件节点冲突，以正式事件和原始剧情为准。

**已确定：编年史采用“客观简史默认 + 史诗编年可切换”的双轨风格。**

第一轨是客观简史，作为默认视图和主要检索入口：

- 每条只写事实、因果、结果、影响范围和指针。
- 使用清晰短句，不写大量修辞、预言口吻或人物内心。
- 便于 AI 快速回溯历史，也便于脚本检查矛盾。
- 可进入 `SAVE.md` 继承摘要、时间线和跨对话恢复包。

第二轨是史诗编年，作为玩家阅读和沉浸展示：

- 由客观简史、正式事件和允许读取的剧情片段派生。
- 可以使用史诗感、神话感、王朝史或宗教史语气。
- 允许按纪元、王朝、文明或灾变整理成可读章节。
- 不允许新增未在事件节点中存在的事实、动机、秘密或因果。

两轨的事实优先级固定为：正式事件节点与原始剧情 > 客观简史 > 史诗编年。史诗编年如果与客观简史冲突，必须标记为风格化错误并重新生成，不得反向修改事实。

前端提供编年史风格切换：

- **客观简史（默认）**：适合查证、恢复、时间线定位和 AI 检索。
- **史诗编年**：适合阅读长历史、纪元回顾和展示世界气质。
- **并排对照**：左侧客观条目，右侧史诗化文本，方便玩家发现偏差。

`update_chronicle.py` 只负责生成和维护客观简史骨架。史诗编年由 `render_chronicle_style.py` 按需生成，结果保存为可重建派生文件；删除史诗编年不会损坏世界事实。

默认编年史是玩家可见的“神之编年史”，可以包含真实因果。未来可另建“凡人史书”，允许记录偏见、谣言和被掩盖的历史，但二者不得混用。

### 6.6 创世设定的来源标记

每个主要创世字段都记录来源，避免以后分不清哪些是玩家指定、哪些是 AI 补全：

```yaml
field: world.magic.source
value: 神力只通过梦境进入凡间
source: player
locked: true
confirmed_at: 2026-08-12T20:00:00+08:00
```

字段来源分为：

- `player_locked`：玩家指定并锁定，普通剧情不得修改。
- `player_editable`：玩家给出初稿，但允许 AI 在预览阶段协助完善。
- `ai_generated`：AI 根据空白字段补全，确认后成为当前正式设定。
- `script_default`：时间格式、坐标范围、ID 规则等机械默认值。
- `divine_revision`：开局后由玩家使用神权主动修改。

AI 补全结果一旦被玩家确认，就不在每次载入时重新生成。重新生成必须由玩家主动选择字段，并保留旧版本和变更记录。

---

## 7. 上下文压缩与跨对话继承

### 7.1 设计原则

不把游戏安全寄托在 Codex 内部何时自动压缩上下文，因为 Skill 无法可靠控制平台内部压缩发生的准确时机。游戏自己维护一套可检查的“工作上下文”和存档机制。

### 7.2 三层记忆

- **热记忆**：当前枝丫的 `SAVE.md` 与 `CURRENT.md`，只保留当前场景、活跃实体、未决问题和最近关键变化。
- **温记忆**：实体卡、近期事件节点和章节摘要，按需要加载。
- **冷记忆**：完整原始剧情、历史事件和旧分支，默认不读取，只沿指针回溯。

### 7.3 检查点策略

每轮结束后记录轮次和新增文本量，并执行启发式检查：

- **普通检查点**：每 8～12 个有效轮次，更新当前枝丫的 `SAVE.md`、`CURRENT.md` 和状态覆盖层。
- **阶段检查点**：一章结束、发生重大世界变化或活跃人物过多时，生成章节摘要并重建索引。
- **换对话预警**：估算工作上下文达到安全阈值时，明确提醒玩家“建议在新对话继续”。
- **强制继承包**：预警时先把当前 `SAVE.md` 归档到本枝 `checkpoints/`，再更新本枝 `SAVE.md`；`ACTIVE.md` 始终指向它。

具体阈值应由实测调整。第一版优先使用“轮次 + 文件新增字符数 + 活跃实体数”的组合判断；如果运行环境能读到可靠的上下文用量，再把它加入判断。

### 7.4 跨对话存档必须包含

1. 世界 ID、当前分支和存档版本。
2. 当前世界时间、天气和焦点地点。
3. 玩家神格、领域、长期命令和显示偏好。
4. 当前场景摘要，以及上一轮停在什么位置。
5. 活跃人物的状态、目标、已知信息和关系变化。
6. 正在推进与等待触发的事件。
7. 关键世界规则和不可违背的事实。
8. 最近关键事件的 ID 与原文指针。
9. 下一轮建议读取的精确文件列表。
10. 随机状态位置，保证换对话后不会重新掷出不同结果。
11. 数据完整性校验结果和异常提示。

枝丫根目录的 `SAVE.md` 是“新对话的快速入口”，不是原始资料的替代品。原始剧情和关键信息文件始终独立保留，旧版存档保存在本枝 `checkpoints/`。

### 7.5 新对话恢复流程

玩家可以说：

> 使用 `$be-a-god` 继续 `worlds/<world-id>/ACTIVE.md`。

也可以直接指定某个枝丫：

> 使用 `$be-a-god` 继续 `worlds/<world-id>/story/main/branches/BR-0001/SAVE.md`。

Skill 应先验证目标 `SAVE.md` 的父级指针和继承边界，再只读取该存档列出的文件与必要父级事件。不得读取任何平级分支。恢复完成后，必须向玩家简短复述：当前分支、分叉点、时间、地点、正在发生的事和上次未完成的选择，等待确认或直接继续。

---

## 8. 随机系统

### 8.1 核心原则

- 随机结果由脚本生成，不由 AI 随口假装随机。
- 每个世界有根种子；创建子分支时由根种子、父分支 ID 和 `fork_event` 派生本枝独立随机流。
- 已用于剧情的结果立即写入 `rng-log.jsonl`，以后不得重新抽取来替换。
- 玩家可以在抽取前指定结果，也可以在抽取后行使神权覆盖。
- 覆盖不会删除原始抽取，而是追加一条“神权改写”记录。
- 平级分支不得共用随机状态或继续读取彼此的随机日志。

### 8.2 随机优先级

```text
玩家指定结果
  > 世界法则或剧情硬约束
  > 条件加权随机
  > AI 对结果的叙事解释
```

### 8.3 第一版随机模块

- 天气：依据地区、季节、气候、上一时段天气和异常神力加权。
- 自然事件：风暴、旱灾、地震、疾病、丰收等。
- 人物倾向：在角色性格、目标和已知信息允许的范围内抽取反应。
- 世界事件：迁徙、冲突、发现、政变、宗教异象等。
- 细节生成：姓名、地名、传闻、梦境、物品特征。
- 概率检定：某个计划是否成功，以及成功代价或失败程度。

随机程序只决定不确定部分，不能让一个忠诚度极高且毫无诱因的人无缘无故背叛。

### 8.4 玩家控制示例

- “让北境立刻下雪，持续七天。”
- “今天的天气照常随机，但不允许出现暴雨。”
- “重新抽取这次商队遭遇；保留第一次结果作为被神抹去的可能性。”
- “今后三个月，这座城发生瘟疫的概率提高到原来的三倍。”
- “隐藏随机数字，只告诉我世界中实际发生了什么。”

### 8.5 已确定的随机展示方式

**已确定：剧情中默认隐藏概率、骰点和内部计算，只呈现世界里实际发生的结果。** 玩家可以随时在前端展开“神之骰”查看完整记录。

“神之骰”面板按当前枝丫展示：

- 随机调用时间、触发原因和关联事件。
- 抽取类型、取值范围、权重与条件修正。
- 原始结果、最终采用结果和对应的世界内表现。
- 玩家是否重抽、指定或使用神权覆盖。
- 随机日志指针与覆盖历史。

提供三种显示偏好：

- **沉浸模式（默认）**：正文只讲实际发生的事，“神之骰”保持折叠。
- **结果模式**：正文后简短显示抽取结果，不展示全部公式。
- **审计模式**：显示权重、修正、原始结果和神权覆盖记录。

玩家可以按单次事件或整局切换显示方式。重抽和覆盖不会删除旧结果；前端只生成操作请求，正式修改仍由脚本写入当前分支的随机日志。兄弟分支的骰点和随机状态不会出现在当前分支面板中。

### 8.6 脚本与大语言模型的职责边界

**已确定：能可靠程序化的机械操作全部封装为脚本；需要开放想象、语义判断或高度随机创作的内容交给大语言模型。**

| 类型 | 执行者 | 例子 |
|---|---|---|
| 确定性机械操作 | 脚本 | 创建目录、生成 ID、推进时钟、移动棋子、检查事件条件、维护队列、写索引、制作存档、验证引用、导出面板 |
| 数值或规则随机 | 脚本 | 天气抽取、成功率检定、路线扰动、遭遇表、资源变化、带种子的概率事件 |
| 高语义创造 | 大语言模型 | 国王的性格与矛盾、宗教神话、人物对白、文化习俗、道德抉择、复杂阴谋 |
| 约束式创造 | 脚本 + 大语言模型 | 脚本给出时代、地区、既有事实与禁止冲突项，模型生成符合约束的新人物或事件 |
| 内容边界执行 | 脚本 + 大语言模型 | 脚本提供内容标签和限制等级，模型按玩家偏好生成或淡化呈现 |

例如创建国王时，脚本只负责分配 ID、读取王国环境和必须满足的约束；国王的性格、欲望、弱点、说话方式和内在矛盾由大语言模型生成。生成结果一旦被玩家接受，就写入人物卡，后续直接读取，不重复生成。

脚本应输出紧凑的结构化结果供 AI 使用，不要求 AI 每次阅读脚本源码。对于可批量处理的索引、结算、校验和导出操作，优先一次调用脚本完成，减少重复的文本推理。

---

## 9. 因果、人物与信息边界

### 9.1 因果结算

重要行动至少检查三种后果：

- **即时后果**：本场景立即发生。
- **延迟后果**：进入事件队列，在未来时间或满足条件时触发。
- **扩散后果**：通过人物关系、经济、宗教、生态或政治传播到其他地区。

AI 不应一次性预写所有未来，而应记录“可能发生什么、触发条件是什么”。这样玩家仍可在中途干预。

### 9.2 人物必须拥有的状态

- 当前地点、身体状态、公开身份和隐秘身份。
- 目标、恐惧、信念、底线和当前计划。
- 对其他人物或势力的关系。
- 亲历事实、听闻信息、误解和完全不知道的事。
- 与玩家神明的信仰、怀疑、恐惧或敌意。
- 最近相关事件 ID 和下一步可能行动。

### 9.3 防止“全员共享 AI 记忆”

角色只能根据自己的知识作出决定。神知道的秘密不会自动泄露给凡人；某人物在远方经历的事，也不会瞬间被其他地区知道。消息传播要有来源、速度和失真。

### 9.4 已确定的死亡、毁灭与神权挽回

**已确定：角色死亡、文明覆灭和世界毁灭在当前分支内都是真实且永久的结果。** AI 不得因为角色重要、剧情尚未完成或叙事方便而自动回滚、复活或假装事件没有发生。

#### 普通死亡与毁灭

- 未被玩家明确赋予不死、保护或锁定命运的角色，都可能依据世界规则真实死亡。
- 重要角色不享有默认“剧情护甲”；其计划、关系和未完成伏笔可以因死亡中断、转移或产生新的连锁事件。
- 死亡必须记录时间、地点、原因、责任者、遗体或灵魂状态，以及关联事件 ID。
- 死者从当前活动棋子列表移除；根据世界规则，地图可保留遗体、墓碑、灵魂或纪念图标。
- 其死亡节点永久保留在纵向时间线和编年史中，查看旧事件时仍能定位其历史位置。
- 已毁灭的地点、文明或世界不能继续按照正常状态行动；相应队列必须取消、转移或标记失效。

#### 世界终局

如果当前世界被彻底毁灭：

- 当前枝丫的 `SAVE.md` 标记为 `ended` 或 `destroyed`。
- 停止普通时间推进和角色自治，但仍允许观察历史、查看编年史和地图遗迹。
- 文件、事件、随机日志和时间线全部保留，不删除该世界线。
- 玩家可以从毁灭前的任意事件创建新枝丫，也可以在当前枝丫发动“重建世界”的绝对神权。

#### 玩家挽回方式

玩家始终保留两种神级处理方式：

1. **当前分支复活或重建**：在死亡或毁灭之后新增神权事件，恢复角色或世界。原死亡/毁灭事件仍是历史事实，其社会、心理、宗教和因果影响不会自动消失。
2. **从事发前创建新枝丫**：以此前事件为 `fork_event` 建立新的 `SAVE.md`，在新世界线中阻止死亡或毁灭；原分支保持不变。

复活后的角色需要记录身体、记忆、年龄、灵魂连续性和他人认知是否恢复。若玩家没有指定，AI 应根据既有世界规则给出最小必要解释，而不是擅自制造额外惩罚。

#### 执行边界

- 死亡或毁灭是否满足条件，由状态、世界规则和随机程序结算；AI 负责合理叙述，不得随意宣布机械结果。
- 世界规则本来允许的复活术可以正常存在，但必须满足明确条件并创建正式事件。
- 玩家锁定的“不死”“不会毁灭”等规则优先于普通死亡判定。
- 普通 AI 推演不能修改已确认的死亡状态；只有明确世界规则、玩家神权或新分支能够改变结果。

---

## 10. 固定事件、条件事件与世界自治

### 10.1 事件类型

- **定时事件**：到达某个世界时间触发。
- **条件事件**：某个状态或组合条件满足时触发。
- **概率事件**：满足条件后按权重抽取。
- **人物行动**：人物为了自己的目标主动执行。
- **神权事件**：由玩家直接创造或修改。
- **连锁事件**：由其他事件的影响进入队列。

### 10.2 世界自治步骤

当玩家推进时间时，系统应：

1. 找出时间范围内到期的固定事件。
2. 更新焦点层、活跃层和背景层实体。
3. 检查人物计划和条件事件。
4. 调用随机程序处理真正不确定的部分。
5. 解决事件冲突与先后顺序。
6. 生成本次推进中值得叙述的事件，不用逐条播报无意义变化。
7. 更新索引、状态、队列和面板数据。

如果本次是跨月或跨年的批量推进，`advance_world.py` 还必须：

- 先创建 `runtime/advance-runs/<advance_run_id>/run.json`。
- 按焦点层、活跃层和背景层分片推进，而不是一次性跳到终点。
- 普通背景变化只写入事件骨架、客观简史或未来队列，不生成完整章节。
- 只把已经发生并结算的事件写入正史；可能未来只能留在队列或虚线时间线节点。
- 遇到死亡、战争、政权更替、关键背叛或玩家决策时生成 `pause.md`，立即停止推进。

暂停包 `pause.md` 至少包含：

- 暂停原因、世界时间、地点和影响范围。
- 关联角色、势力、地点、事件 ID 和随机记录 ID。
- 已经写入正史的事实。
- 尚未结算的选择和风险。
- 玩家可选行动。
- 继续推进时应使用的推进游标。

### 10.3 地图棋子与角色漫游

**已确定：能够行动的角色以棋子的形式持续显示在地图上。** 即使角色暂时没有明确任务、正在漫无目的地漫游，也要保留可见位置，让玩家容易发现、观察和介入。

每个地图棋子至少具有：

- 角色 ID、显示名称、头像或简化棋子图标。
- 当前归一化坐标、所在地点和上次更新时间。
- 行动状态：静止、漫游、目标移动、剧情就绪、事件中、被跟随、隐藏。
- 行进路线或下一目的地；漫游者可以只有活动范围。
- 故事热度与提醒级别。
- 最近事件、可触发事件和角色卡指针。

棋子状态建议使用清晰但克制的视觉区别：

- 普通棋子：静止或日常行动。
- 缓慢移动动画：正在漫游或赶路。
- 光环或脉冲边框：已经具备故事触发条件。
- 感叹号：即将发生且可能需要玩家决定的高优先级事件。
- 锁定环：玩家正在跟随的角色。

点击棋子时先打开轻量卡片，只显示当前位置、眼前状态、最近变化和可执行操作。玩家选择“观察”“对话”“跟随”“降下神谕”或“触发故事”后，再生成最小交互包，不预先加载人物的完整历史。

棋子移动由脚本根据世界时间、路线、速度、地形和角色计划计算。没有目标的漫游也由受活动范围约束的随机程序完成，不能瞬间跨越不合理距离。高语义的“为什么离开”“途中想到了什么”“遇见后如何反应”仍由大语言模型创作。

角色满足条件并准备发生故事时，先把棋子标记为“剧情就绪”。是否立即进入剧情、等待玩家点击、继续后台演化或自动暂停，由该世界的推进预设决定。

### 10.4 已确定的自动暂停与角色忽略

#### 默认自动暂停

**已确定：以下事件默认暂停自动推进并提醒玩家：**

- 角色死亡或世界/文明毁灭。
- 战争正式爆发。
- 政权更替、继位或大规模叛乱。
- 关键角色背叛或阵营转换。
- 必须由玩家作出选择才能继续的事件。

玩家可以在创世弹窗和常驻看板中逐项启用、关闭或调整提醒级别。世界毁灭、规则冲突和“缺少玩家决定无法继续”的硬阻塞事件仍必须给出最小系统提示，防止推进状态无声卡死。

#### 角色关注状态

**已确定：每个命名角色提供“关注 / 普通 / 忽略”三态按钮。**

- **关注**：棋子醒目显示；个人重要事件可以自动暂停并优先进入故事。
- **普通**：遵循世界的默认暂停和提醒规则。
- **忽略**：不弹出该角色的个人剧情，不因其普通个人事件暂停，也不实时生成冗长叙事；直到玩家主动点击该角色才展示其故事。

关注状态记录在当前枝丫的 `SAVE.md` 和状态覆盖层中。不同世界线可以拥有不同关注状态，不向平级分支传播。

#### 忽略期间如何运行

- 角色仍在世界里正常移动、计划、交往、衰老、受伤或死亡，不能因为被忽略而冻结。
- 地图棋子继续存在，但降低视觉亮度并显示“已忽略”标识；角色死亡后按死亡规则处理。
- 机械变化、状态差异和正式事件仍然记录，但只保存紧凑事件骨架，不即时生成完整对白和场景描写。
- 必须进行高语义决策时，AI 只读取该角色的小型决策包并保存结论，不扩写成玩家当前看不到的长篇故事。
- 该角色的个人时间线节点默认折叠为“被忽略动态”，不暴露具体剧情。
- 如果该角色造成战争、政变、灾害等影响整个世界的事件，系统仍呈现必要的世界结果；但不自动展开其个人动机和幕后过程。
- 单纯属于该角色个人的死亡、恋爱、旅行、争执或发现不会打断玩家，除非它同时触发了玩家未关闭的世界级事件。

#### 玩家主动点击后

玩家点击被忽略角色时：

1. `build_ignored_digest.py` 只收集该角色从 `ignored_since` 起的事件骨架与相关状态差异；事件归属按 `actors` 或旧版 `target_id` 元数据判断，不按正文提到的 ID 判断。
2. AI 先生成一份紧凑的“你忽略期间发生了什么”摘要，不读取无关历史。
3. 前端展开该角色此前折叠的时间线节点，并联动相关地图位置。
4. 玩家可以继续查看某个具体事件、开始对话、改为普通或设为关注。

点击只代表查看，不自动取消“忽略”。角色会继续保持忽略状态，直到玩家明确点击“取消忽略”或切换为普通/关注。

---

## 11. 前端“神之视界”面板

### 11.1 第一版范围

前端是本地轻量网页，承担创世入口和世界观察，但不作为唯一事实来源。

- 新建世界按钮与创世填空弹窗。
- 世界地图或区域示意图。
- 按坐标显示事件图钉。
- 按实时世界状态显示角色棋子、行进方向和漫游范围。
- 醒目标记“剧情就绪”及需要玩家决定的角色棋子。
- 点击棋子快速执行观察、对话、跟随、神谕或触发故事。
- 角色卡和棋子快捷菜单提供“关注 / 普通 / 忽略”按钮。
- 点击图钉查看事件摘要、时间、参与者和相关事件。
- 固定展示可上下滚动的纵向世界时间线，可筛选地区、人物、势力和事件类型。
- 常驻看板：世界时间、季节、天气、当前分支、焦点地点、活跃危机。
- 重要人物/势力状态卡。
- “最近发生”“即将可能发生”“等待玩家决定”三个列表。

### 11.2 创世填空弹窗

玩家点击“新建世界”后，前端以分步弹窗呈现创世模板。

#### 第一步：必要内容

1. **一句话世界构想**：玩家想体验怎样的世界或故事。
2. **玩家的神明定位**：已有神格/领域，或选择“由 AI 提议”。
3. **世界观察尺度**：城市、王国、大陆、整颗星球或自定义。
4. **内容边界**：选择预设分级，填写不想出现的内容，或逐项自定义呈现强度。

必要字段不能悄悄跳过，但除“一句话世界构想”外，可以明确选择“让 AI 提议”。

#### 内容边界与呈现强度

**已确定：前端提供预设分级、一键淡化和逐项自定义。** 该设置既用于创世阶段，也用于后续叙事、摘要和事件展示。

预设分为四档：

- **温和**：弱化暴力、恐怖、灾难和强烈情绪压迫；死亡、战争和灾害主要以结果摘要呈现。
- **标准（默认）**：允许正常故事冲突和严肃后果，但避免无必要的细节堆砌。
- **不主动淡化**：在允许范围内不额外弱化战争、阴谋、死亡、背叛和文明崩坏等内容。
- **自定义**：玩家按类型单独设置限制。

“不主动淡化”不是越过 Codex 或平台安全边界的开关，只表示游戏系统本身不再额外降低叙事强度。所有生成仍必须服从当前运行环境和 Skill 的安全规则。

自定义面板至少包含以下类型：

- 暴力与伤害。
- 死亡、疾病和灾难。
- 恐怖、怪诞和精神压力。
- 情感冲突、背叛和亲密关系。
- 社会压迫、宗教冲突和族群矛盾。
- 玩家自填的绝对禁区。

每个类型提供四种处理方式：

- **允许**：可作为剧情内容出现。
- **淡化**：事件可以发生，但描述更克制。
- **只摘要**：只保留事实后果，不展开场景。
- **禁止**：不得由 AI 主动生成；如果世界规则即将导向该内容，必须暂停并询问玩家。

前端提供“淡化本段”按钮。它只重新生成当前段落的呈现方式，不改变正式事件、死亡、伤害、战争结果或编年史事实。玩家之后也可以修改整局内容边界；修改只影响未来生成和可选的重新摘要，不会自动改写已经发生的历史。

内容边界写入 `setup/content-profile.json`，关键偏好同步摘要到 `PLAYER.md`。每次创作或摘要前，脚本只把必要的边界标签装入小型上下文包，避免 AI 为了判断呈现强度反复读取完整玩家偏好。

#### 第二步：可选指定内容

- 世界名称、时代和科技水平。
- 地理、气候、地图形态与重要地点。
- 种族、物种、文明与语言。
- 魔法、神力、超自然现象和世界法则。
- 国家、势力、宗教与社会结构。
- 指定角色、关系、物品或传说。
- 故事基调、叙事风格和希望出现的主题。
- 开局年代、地点、危机或历史事件。
- 必须存在、必须发生、绝对禁止改变的内容。

每一项旁边都有三态控制：

- **锁定**：严格按玩家填写内容创建。
- **可润色**：保留核心意思，允许 AI 改善表达和补充细节。
- **AI 补全**：该项留白并交给 AI 创建。

#### 第三步：生成与确认

提交表单后按以下流程处理：

1. 脚本验证必填项、字段类型和互相冲突的锁定条件。
2. 将玩家原话和锁定状态保存为 `WORLD-BRIEF.md` 与 `world-spec.json`。
3. AI 只针对空白和“可润色”字段生成世界草案，并先写入 `setup/drafts/`。
4. 脚本检查草案是否违反锁定事实，补齐 ID、坐标、引用和初始文件结构。
5. 前端显示对照预览，明确区分玩家指定、AI 补全和程序默认。
6. 玩家可以确认全部、手动修改，或只重新生成指定的未锁定字段。
7. 玩家最终确认后，`init_world.py` 才把 `setup/drafts/` 中的草案写成正式世界。

如果前端没有运行本地配套服务，弹窗仍能把表单导出为 Markdown/JSON 或复制成 Codex 指令；有本地服务时，也只能先写入创世草案区，不能绕过确认直接覆盖正式世界。

### 11.3 数据原则

- 前端读取由脚本生成的 `dashboard/data.json` 与 `dashboard/timeline.json`。
- 真实事实仍来自 Markdown 事件、实体和规则文件。
- 面板缓存损坏时可以重新导出，不会损坏存档。
- 地图坐标统一使用 0～100 的归一化坐标，方便以后更换地图图片。

### 11.4 交互建议

除经过确认的创世提交外，第一版的普通操作按钮不直接改写正式游戏文件，而是生成一条可以复制给 Codex 的自然语言命令，例如：

> “观察河谷城的 EVT-000142，并把时间推进到第二天清晨。”

后续如果需要更强的实时控制，再加入本地服务和安全的写入接口。

### 11.5 第二阶段可选功能

- 时间播放和世界自动推进。
- 人物关系图与因果链视图。
- 多世界线对比。
- 神力、信仰、人口、资源和灾害趋势图。
- 在地图上圈选区域后下达范围神谕。

### 11.6 图钉与棋子的区别

- **事件图钉**表示“这里发生过或正在发生什么”，主要连接编年史和事件节点。
- **角色棋子**表示“谁现在在哪里、正在做什么”，会随世界推进移动。

一个角色进入事件时，棋子可以与事件图钉产生连接，但二者不能合并成同一种数据，以免历史位置和当前位置相互覆盖。

### 11.7 已确定的三级地图

**已确定：第一版采用三级语义缩放地图。** 标准层级为：

```text
世界 / 大陆
└─ 地区
   └─ 城镇 / 具体场景
```

如果玩家创建的世界本身较小，层级名称可以映射为“王国 → 城镇 → 建筑/场景”；如果世界尺度更大，可以映射为“星球 → 大陆 → 地区”。无论名称如何变化，第一版始终保持三级，避免无限嵌套。

#### 一级：世界或大陆

- 展示大地理轮廓、主要国家/势力、宏观天气和重大危机。
- 普通角色按地区聚合为棋子组，不逐个铺满地图。
- 被玩家跟随、即将触发剧情或处于重大事件中的角色，始终单独显示。
- 点击地区进入二级地图。

#### 二级：地区

- 展示城镇、道路、边界、地形、区域事件和角色移动方向。
- 展示重要角色棋子；普通角色可以按地点聚合。
- 显示角色的漫游范围、目标路线和预计抵达时间。
- 点击城镇或局部区域进入三级地图。

#### 三级：城镇或具体场景

- 展示单个角色棋子、关键物体、建筑、环境状态和当前事件。
- 支持点击角色对话、观察、跟随、降下神谕或触发故事。
- 支持点击环境或物体进行快速交互。
- 只在这一层显示细粒度位置，避免在世界层处理无意义的小范围移动。

#### 棋子显示优先级

当同一位置棋子过多时，按照以下优先级保证可见性：

1. 玩家锁定或正在跟随的角色。
2. 剧情就绪、即将死亡、继位、开战或等待玩家决定的角色。
3. 正在执行重要计划或参与当前事件的角色。
4. 普通命名角色。
5. 无名人群和背景单位，允许聚合为数量棋子。

剧情就绪角色不能因为地图缩小或棋子聚合而被隐藏；至少以独立提示、边缘指示器或聚合组上的醒目标志出现。

#### 坐标与缩放规则

- 每个地点保存 `parent_id`、地图层级和父地图内 0～100 的局部坐标。
- 全局位置由脚本沿父级坐标计算，AI 不手工换算坐标。
- 角色跨地区移动时保存路径节点、出发时间和预计到达时间。
- 缩放、平移和棋子聚合只读取 `dashboard` 导出的地图数据，不加载剧情正文。
- 点击角色或事件后，才根据 ID 生成最小交互包或事件摘要。
- 地形、城市或国界被神力改变时，只在当前枝丫创建新地图版本，不静默覆盖旧历史，也不影响平级分支。

#### 地图生成分工

- AI 负责地理概念、地区特色、文化意义、地点名称及合理的空间关系。
- 脚本负责三级层级、ID、坐标布局、父子引用、路径连通、碰撞避让和数据导出。
- 第一版使用轻量矢量示意地图，不要求复杂写实地形；以后可以替换地图美术，但不得改变地点 ID 和事件指针。

### 11.8 已确定的地图与棋子美术

**已确定：第一版采用“纸质地图 + 神性星盘”双主题，棋子采用抽象徽章。** 视觉主题只影响前端展示，不改变地图层级、坐标、地点 ID、事件指针或分支事实。

#### 地图主题

第一版提供两个主题：

- **纸质地图（默认）**：适合王国、大陆、城镇、航海、战争和探索类世界。视觉基调是手绘轮廓、墨线边界、地名标签、地形纹理和温和的状态色。
- **神性星盘**：适合宏观观察、命运线、神权干预、多世界线和超自然世界。视觉基调是星图轨道、命运连线、分支辉光、时间刻度和神谕标记。

玩家可以在前端切换主题。第一版主题切换只改变前端本地展示状态（例如浏览器 `localStorage`），不触发剧情推进，不读取剧情正文，也不写入 `PLAYER.md`、`dashboard/data.json` 或 `dashboard/timeline.json`。当前分支前端缓存可随时由脚本重建，但主题偏好不属于世界事实。

纸质地图和神性星盘必须共享同一套坐标与图层数据：

- 纸质地图把地点显示为地理位置。
- 神性星盘把地点显示为命运节点或观察节点。
- 事件图钉、角色棋子和时间线定位在两个主题中保持同一 ID。
- 主题切换后，玩家当前选中的角色、事件、缩放层级和时间线位置保持不变。

#### 棋子表现

第一版使用抽象徽章，不默认使用 AI 头像。原因是棋子需要在世界层、地区层和场景层稳定缩放，头像容易造成视觉噪声，也会增加资产生成和一致性成本。

抽象徽章由四类信息组成：

- **身份形状**：人物、势力、地点守卫、群体单位、灵魂/遗迹等使用不同外形。
- **阵营颜色**：来自势力、信仰或地区，不直接由 AI 即兴选择。
- **状态环**：跟随、剧情就绪、被忽略、危险、死亡、移动中、等待玩家决定等状态用环线或角标显示。
- **短标记**：最多 1～2 个汉字、首字母或图标，用于区分关键角色。

状态显示优先级：

1. 等待玩家决定、剧情就绪、死亡/毁灭风险。
2. 玩家跟随、玩家锁定、神权保护。
3. 被忽略、折叠、后台演化。
4. 普通移动、停留、漫游、参与事件。

被忽略角色的棋子不消失，只降低亮度并加折叠标记。剧情就绪角色必须有醒目脉冲或边缘提示。死亡角色不再作为活动棋子移动；如果世界规则允许遗体、墓碑、灵魂或残影存在，则显示为对应的非活动徽章。

#### 以后可扩展

- 头像徽章作为可选增强，只用于角色卡、近景互动或少量关键人物。
- 主题可以继续扩展为黑曜石棋盘、文明沙盘、史诗羊皮卷等，但必须复用同一套地图与棋子数据协议。
- 若增加头像或插画，资产应存入 Skill 的 `assets/` 或世界目录的可重建缓存，不应成为事实来源。

### 11.9 已确定的纵向世界时间线

**已确定：前端必须包含类似文明类游戏的纵向时间线。** 它是常驻核心界面，不是需要另开页面才能看到的附属列表。

#### 基本布局

- 时间线以纵向轨道呈现，支持鼠标滚轮、触控拖动和滚动条上下浏览。
- 上方是更早的历史，下方是更晚的历史；当前世界时间使用醒目的“现在”横线标记。
- 提供“回到现在”按钮，点击后立即定位当前世界时间。
- 纪元、世纪、年份、月份等时间刻度根据缩放级别自动合并，避免长世界史挤成一团。
- 用户的滚动位置、筛选条件和展开状态保存在前端偏好中。

#### 时间线内容

- 纪元开始与结束。
- 已确认的重要事件、神权干预、战争、灾害、继位、文明发现和角色命运转折。
- 世界线分叉点及当前分支名称。
- 当前正在发生的事件。
- 经玩家允许显示的定时事件和可能未来。

显示样式必须区分事实状态：

- **实线节点**：当前分支已经发生并写入正史的事件。
- **高亮节点**：当前正在处理的事件。
- **分叉节点**：产生新 `SAVE.md` 枝丫的历史分叉点。
- **虚线节点**：尚未发生的计划、条件或概率事件，不能被当作正史。
- **锁定标记**：玩家明确指定、不可由普通推演改变的事件。

#### 地图与棋子联动

- 点击时间线事件，地图自动定位相应层级、地点和事件图钉。
- 若事件涉及角色，相关棋子高亮；死亡或已离场角色显示其事件发生时的位置，不伪造当前位置。
- 点击地图图钉时，时间线同步滚动到对应事件。
- 点击条目先显示紧凑摘要；只有玩家选择“查看原始剧情”时，才沿 `source` 读取正文。
- 玩家可以从某个历史节点发起“从这里创造新分支”，随后生成该枝丫的 `SAVE.md`。

被忽略角色的纯个人事件默认不逐条展开，只显示一个可点击的折叠节点和累计数量。玩家点击该角色后，时间线才按需展开其历史；已经影响世界的宏观事件仍以世界事件显示，但不会泄露被忽略的个人叙事。

#### 分支隔离

时间线只合成：

1. 父级血统链中位于各自 `inherit_until` 之前的事件。
2. 当前枝丫分叉后自身发生的事件。

平级分支不会混入。只有玩家进入“比较世界线”功能时，才允许并排查看其他分支，而且比较视图不改变当前正史。

#### 长时间线性能

- `build_timeline.py` 从当前分支事件和编年史生成 `dashboard/timeline.json`。
- 前端只渲染视口附近的时间节点，历史规模很大时也不一次加载全部事件卡片。
- 宏观缩放时按纪元或年份聚合；放大后再显示月份、日期和具体场景。
- 重建时间线只处理新增或变化的节点，不重复让 AI 总结全部历史。

---

## 12. Skill 包结构草案

```text
be-a-god/
├─ SKILL.md
├─ agents/
│  └─ openai.yaml
├─ scripts/
│  ├─ init_world.py              # 从确认后的草案创建正式世界
│  ├─ create_world_brief.py       # 生成创世填空草案
│  ├─ create_action_request.py    # 前端/玩家动作先登记为 support request
│  ├─ make_interaction_packet.py  # 为即时交互装配最小上下文
│  ├─ advance_world.py            # 推进时间、队列暂停和漫游
│  ├─ create_branch.py            # 在故事树中创建带 SAVE.md 的子枝
│  ├─ switch_branch.py            # 确认后切换 ACTIVE.md 并重建分支缓存
│  ├─ validate_world.py           # 检查矛盾、断链、摘要和派生数据
│  ├─ check_install_ready.py      # 安装预检
│  └─ ...                         # 其余脚本详见 references/script-catalog.md
├─ references/
│  ├─ game-master-protocol.md    # 每轮运行与叙事规则
│  ├─ storage-contract.md        # 文件格式、ID、分支、存档和安全边界
│  ├─ frontend-contract.md       # 前端只读面板、动作请求和主题契约
│  ├─ script-catalog.md          # 脚本目录与职责
│  └─ validation-scenarios.md    # 可玩前验收场景
└─ assets/
   ├─ world-template/            # WORLD-BRIEF、SAVE、内容/推进配置模板
   └─ frontend-template/         # 静态只读前端、样例 dashboard/timeline/map 数据
      ├─ index.html
      ├─ styles.css              # 纸质地图 / 神性星盘主题样式
      └─ app.js                  # 创世弹窗、时间线、地图棋子和动作请求
```

最终 `SKILL.md` 只保留启动方式、每轮核心流程和按需读取哪些参考资料，详细规则放入 `references/`，避免 Skill 自身每次触发就占用大量上下文。

### 12.1 建议调用方式

- `$be-a-god 创建一个新世界`
- `$be-a-god 继续上次的存档`
- `$be-a-god 观察北境，不推进时间`
- `$be-a-god 让世界自然运行三个月`
- `$be-a-god 检查当前世界是否存在设定冲突`
- `$be-a-god 为当前进度生成跨对话继承存档`

自然语言仍是正式入口，不要求玩家死记固定参数。

---

## 13. 数据安全与一致性

### 13.1 不可静默发生的操作

- 删除或覆盖原始剧情。
- 修改已经确认的随机结果。
- 把摘要中的推测当成正式事实。
- 合并世界线却不记录来源。
- 改变人物知识却不记录信息来源。
- 因为 AI 忘记而复活已死亡人物、重置关系或重复触发事件。

### 13.2 每次大存档的验证

- 当前分支和时间是否合法。
- `CURRENT.md` 是否与最新事件一致。
- 活跃实体引用的事件是否存在。
- 事件 `source` 是否能定位到原始剧情。
- 是否存在同一人物同时出现在冲突地点。
- 已完成事件是否仍错误地留在待触发队列。
- 随机日志位置是否与存档一致。
- 前端数据是否为当前存档版本导出。

### 13.3 恢复策略

索引、面板数据和 `CURRENT.md` 都可以从历史重建。原始剧情、事件节点、神谕记录和随机日志是恢复所需的核心资料，因此应采用只追加与版本化存档。

### 13.4 玩家手动编辑 Markdown 的处理规则

**已确定：玩家手动修改 Markdown 后，系统不自动接受为正史。** 处理流程必须是“检测差异 → 显示影响范围 → 玩家确认 → 合并并重建派生数据”。

允许玩家直接阅读和编辑 Markdown，但下一次继续游戏、恢复存档或运行一致性检查时，Skill 必须先执行手动编辑检测：

1. `detect_manual_edits.py` 对比 `system/file-manifest.json`、最近检查点和当前文件内容。
2. 将变化按文件、标题、实体 ID、事件 ID 和字段类型归类。
3. 标记风险等级，并在当前枝丫写入 `runtime/manual-edit-reports/<report_id>/report.md` 与 `merge-plan.json`。
4. Codex 向玩家展示简短影响范围，不直接把改动纳入正史。
5. 玩家可以选择全部接受、部分接受、放弃某些修改，或要求解释冲突。
6. 玩家确认后，`apply_manual_edits.py` 按报告 ID 创建检查点，合并修改，重建索引、时间线、面板数据和验证报告。
7. 合并过程写入 `system/manual-edit-ledger.jsonl`，记录来源、时间、影响文件、接受/拒绝项、玩家确认文本和合并后的文件指纹。
8. 合并成功后刷新 `system/file-manifest.json`，避免同一批手改在下一轮被重复提示。

手动改动分为三类：

- **低风险文字修改**：错别字、描述润色、注释补充、史诗编年展示文本。可以批量接受，但仍要记录。
- **中风险事实修改**：人物状态、地点信息、关系、队列、客观简史、`CURRENT.md` 或 `SAVE.md` 摘要。必须显示受影响实体和事件。
- **高风险结构修改**：事件 ID、父分支指针、`fork_event`、随机日志、死亡/复活事实、已确认神谕、地图坐标、`CANON.md` 锁定规则。必须逐项确认，并在必要时建议创建新分支。

以下内容不能静默手改为正史：

- 删除或改写原始剧情章节。
- 修改已确认随机日志或神权覆盖记录。
- 改变父分支、分叉事件或继承边界。
- 复活死者、撤销世界毁灭、取消战争结果等已发生重大事实。
- 把史诗编年中的修辞或传说写回客观事实。

如果手动修改只影响派生文件，例如索引、前端缓存、史诗编年或 `CURRENT.md`，系统优先从核心资料重建，而不是把派生文件当作权威来源。若玩家确实想修改正史，应改事件节点、人物卡或使用一次明确的神权修订。

当手动修改会改变已发生历史时，默认路线不是覆盖原分支，而是询问玩家是否创建新枝丫；只有玩家明确声明“修正错录事实”且相关事件、指针和状态能自洽时，才允许在当前分支合并。

事实性手动修改一旦被接受，必须留下可追溯事件：

- 如果只是修正错录、错别字或文件搬运错误，写入 `manual-correction` 事件，说明原记录、修正内容、影响范围和确认文本。
- 如果玩家把手改当作一次神权干预，默认创建新枝丫；若玩家明确要求留在当前枝丫，则写入 `divine-revision` 事件并重新结算受影响状态。
- 如果只修改派生展示文本，不创建事实事件，只记录到 `manual-edit-ledger.jsonl` 并重建派生文件。

合并报告必须列出三组结果：`accepted_changes`、`rejected_changes`、`blocked_changes`。凡是事件 ID、父指针、随机日志、死亡事实或锁定设定存在冲突且无法自动证明自洽的项目，一律进入 `blocked_changes`，不能被部分静默合并；Codex 必须把阻塞项交还玩家确认或改用新枝丫方案。

---

## 14. 第一版可玩范围（MVP）

### 14.1 必须实现

1. Skill 可被 Codex 发现和调用。
2. 能创建一局游戏的完整文件目录。
3. 能以自然语言进行观察、干预和时间推进。
4. 能保存完整剧情与结构化关键事实。
5. 能建立人物、地点、势力和事件之间的指针。
6. 能调用可复现随机程序生成天气和通用事件。
7. 玩家能覆盖任意随机结果，并保留覆盖记录。
8. 能定期更新当前枝丫的 `SAVE.md` 与 `CURRENT.md`，并归档检查点。
9. 新对话能从 `ACTIVE.md` 或任意枝丫的 `SAVE.md` 恢复，不必读取全部历史。
10. 能检查常见矛盾、断链和重复事件。
11. 能使用玩家自定义的混合推进预设，并在高优先级事件处暂停。
12. 能在地图显示静止、漫游、移动和剧情就绪的角色棋子。
13. 能点击棋子后通过最小交互包快速观察、对话或干预。
14. 能从正式事件自动增量维护世界编年史。
15. 能通过脚本完成机械结算，把高语义创作留给大语言模型。
16. 能通过前端填空弹窗收集必要内容、可选指定内容和字段锁定状态。
17. 能由 AI 补全未锁定设定，显示来源对照，并在玩家确认后才创建正式世界。
18. 能在世界/大陆、地区、城镇/场景三级地图间缩放，并保持地点、角色和事件引用一致。
19. 能根据地图观察、棋子交互和时间跨度自动切换全知、近景与编年史视角，并允许玩家覆盖。
20. 改变过去时能在主故事树中创建带 `SAVE.md` 的子枝，只沿父级回溯，并与平级分支完全隔离。
21. 前端必须常驻显示可上下滚动的纵向世界时间线，并与地图、图钉、棋子和分支节点联动。
22. 随机结果默认在剧情中隐藏，同时能从“神之骰”面板审计和覆盖。
23. 角色、文明和世界能在当前分支真实死亡或毁灭，同时允许玩家通过新神权事件或事前分支进行挽回。
24. 自动推进默认在死亡、毁灭、战争、政权更替、关键背叛和玩家决策处暂停，并允许玩家调整。
25. 玩家能把角色设为忽略；其世界状态继续演化，但个人故事只在玩家主动点击后按需展示。
26. 前端能设置内容边界预设、逐项限制和一键淡化，并把设置写入 `content-profile.json`。
27. 前端能在纸质地图和神性星盘主题间切换，并用抽象徽章显示角色棋子状态。
28. 编年史默认维护客观简史，并能按需渲染史诗编年展示文本。
29. 能检测玩家手动修改 Markdown 后的差异，显示影响范围，并在玩家确认后合并。

### 14.2 MVP 后再做

- 更复杂的地形地图编辑器与范围圈选神谕。
- 对话关闭后的常驻后台世界运行。
- 多条平行世界线同时模拟。
- 向量检索。
- 复杂经济、战争或生态模型。
- 多玩家共同扮演不同神明。

先验证“长剧情能稳定继承、事实不乱、随机可控”之后，再扩展前端和复杂模拟。

---

## 15. 验收场景

开发完成后至少通过以下测试：

1. 创建世界后，所有必要文件存在且初始状态一致。
2. 连续推进 50 轮，原始剧情只追加，实体状态能正确更新。
3. 询问第 3 轮某人物的承诺时，只回溯相关节点和原文片段。
4. 用同一存档重放随机调用，得到相同结果。
5. 玩家覆盖天气后，旧随机结果仍可审计，当前天气以神谕为准。
6. 在预警点更新当前枝丫 `SAVE.md`，新对话能够从该文件无缝继续。
7. 修改过去时能创建带父级指针和分叉事件的子枝，不破坏旧历史。
8. 删除自动索引后能够从核心资料重建。
9. 面板能正确显示当前时间、天气和事件图钉，且不成为事实源。
10. 人物不会利用其从未获得的秘密作出决定。
11. 同一次推进中，焦点地区能逐场景演化，背景地区能按月汇总，并在预设的重要事件处暂停。
12. 无目标角色能在合法活动范围内漫游，其棋子始终可见且不会发生不合理瞬移。
13. 角色满足故事条件时，棋子能切换为“剧情就绪”，点击后用最小交互包触发剧情。
14. 普通对话只读取当前场景与目标实体的必要信息；涉及旧承诺时才沿指针补读对应原文。
15. 每个正式重要事件都能自动进入编年史，并能从条目返回事件节点和剧情原文。
16. 推进时钟、棋子移动、索引和存档可以由脚本直接完成；国王性格等高语义内容由模型生成且确认后不重复生成。
17. 创世弹窗的锁定字段能逐字进入正式设定，AI 只补全允许生成的字段。
18. 只重新生成一个未锁定字段时，其他已确认内容保持不变，旧版本仍可追溯。
19. 玩家未确认创世草案时，系统不会把它误当作正式世界开始游玩。
20. 从世界层缩放到场景层时，棋子位置能正确继承，缩放过程不读取剧情正文。
21. 大量棋子聚合时，剧情就绪和玩家跟随角色仍能单独显示或给出醒目指示。
22. 神力改变地形后，地图生成新版本，旧事件仍能定位到其发生时的地图状态。
23. 从世界地图点击角色棋子时，叙事能从全知视角切入人物近景，结束后返回原地图层级。
24. 跨年推进使用编年史概述；遇到高优先级人物事件时能暂停并切入近景。
25. 玩家看到某个秘密后，不知情角色仍不会利用该秘密；切换视角也不会触发无关历史读取。
26. 子枝能回溯父分支截至 `fork_event` 的事件，但不会继承父分支后来新增的事件。
27. 两个平级分支分别推进后，人物、事件队列、地图、编年史和随机流不会相互污染。
28. 切换到另一枝丫时，`ACTIVE.md`、索引和前端数据能全部重建为目标分支状态。
29. 普通检索不会扫描兄弟分支；只有玩家明确要求切换或比较时才允许读取。
30. 时间线能上下滚动、回到现在并按纪元或日期缩放，长历史不会一次渲染全部事件卡片。
31. 点击时间线事件能定位地图图钉和相关棋子；点击图钉也能反向定位时间线条目。
32. 当前分支时间线只包含父级继承范围与本枝事件，不会混入任何平级分支。
33. 未发生事件使用虚线并明确标为可能未来，不会被编年史或 AI 当成正式历史。
34. 沉浸模式正文不显示骰点；展开“神之骰”后能查看原始结果、修正与神权覆盖记录。
35. 重要角色死亡后会停止行动、退出活动棋子列表，并在事件、编年史和时间线上永久留痕。
36. 没有神权、世界规则或新分支依据时，AI 不会擅自复活死者或恢复已毁灭文明。
37. 玩家在当前分支复活角色时，会新增复活事件；原死亡节点和已经发生的影响仍然保留。
38. 世界毁灭后该枝丫停止普通推进但仍可查看，并能从毁灭前节点创建不影响原枝的子分支。
39. 默认自动推进在死亡、战争、政权更替、关键背叛或玩家决策发生时能正确暂停。
40. 被忽略角色仍会移动和产生真实状态变化，但其普通个人事件不会弹窗或暂停推进。
41. 忽略期间只记录事件骨架，不生成无用长篇场景；点击角色后才生成紧凑回顾。
42. 点击被忽略角色能展开其折叠时间线并定位相关地图事件，但不会自动取消忽略状态。
43. 被忽略角色造成世界级事件时，玩家能看到必要世界结果，同时个人动机仍保持折叠。
44. 当前分支的角色关注状态不会影响平级分支中的同一角色。
45. 创世时选择“温和 / 标准 / 不主动淡化 / 自定义”后，设置能写入 `content-profile.json` 并同步到 `PLAYER.md` 摘要。
46. 被设为“只摘要”的内容可以保留世界事实和后果，但不会展开成场景描写。
47. 被设为“禁止”的内容即将由规则触发时，系统会暂停并请求玩家处理，而不是绕过限制继续生成。
48. 点击“淡化本段”只改变当前输出的表达强度，不改写事件节点、状态变化、随机日志或编年史事实。
49. 在纸质地图和神性星盘之间切换时，当前选中角色、事件图钉、缩放层级和时间线位置保持不变。
50. 主题切换只读取前端导出数据，不加载剧情正文，也不改变任何正式存档。
51. 抽象徽章能区分人物、势力、群体、非活动遗迹等类型，并能显示跟随、剧情就绪、忽略、危险、死亡和移动状态。
52. 被忽略角色在地图上降低亮度但不消失；剧情就绪角色不会被普通聚合隐藏。
53. 客观简史条目能作为检索、恢复和时间线定位入口，并保留事件 ID 与原文指针。
54. 史诗编年可以从客观简史派生，但不能新增事实、动机、秘密或因果。
55. 当前端切换编年史风格时，只切换展示文本，不改变事件节点、客观简史、时间线或存档。
56. 删除或重建史诗编年文件后，世界事实和客观简史仍保持完整。
57. 玩家手动修改普通描述后，系统能检测差异、显示低风险报告，并在确认后记录到 `manual-edit-ledger.jsonl`。
58. 玩家手动修改人物状态或 `SAVE.md` 摘要后，系统能列出受影响的人物、事件、时间线和派生文件。
59. 玩家手动修改事件 ID、父分支指针、随机日志或死亡事实时，系统必须标记高风险并逐项确认。
60. 手动修改派生文件时，系统优先从核心资料重建，不把派生文件反向当成事实来源。
61. 手动修改会改变已发生历史时，系统默认建议创建新枝丫；只有玩家明确确认“修正错录事实”时才尝试当前分支合并。
62. 手动修改合并后，索引、时间线、前端数据和验证报告能全部重建并保持一致。

---

## 16. 仍需逐步确定的设计问题

按优先级排列：

核心机制问题已暂时全部确定。下一步不再继续抽象补规则，而是用真实游玩片段验证规则是否够用。

优先验证：

1. 创世填空到正式世界初始化。
2. 一次普通近景互动。
3. 一次跨月或跨年编年史推进。
4. 一次改变过去并创建子枝。
5. 一次玩家手动修改 Markdown 后的差异检测和合并。

### 16.1 真实游玩片段验证方案

验证目标不是写出好看的故事，而是确认文件、指针、检索、分支、前端数据和玩家确认边界能连起来。每个片段都要记录：玩家输入、读取文件、写入文件、生成事件、前端变化、是否触发暂停、是否需要玩家确认。

#### 片段 1：创世填空到正式世界初始化

玩家输入示例：

> 创建一个王国级世界：古老河谷王国正在衰败，玩家是掌管河流与誓约的神。尺度选择“王国”，内容边界选择“标准”，地图默认纸质地图，未填内容由 AI 补全。

必须验证：

- 创世弹窗字段能转成 `WORLD-BRIEF.md`、`world-spec.json` 和 `content-profile.json`。
- 玩家锁定内容进入 `CANON.md` 或世界初始规则，不被 AI 补全覆盖。
- AI 只补未锁定字段，并在预览中标明来源。
- 玩家确认前不创建正式世界；确认后才运行 `init_world.py`。
- 生成 `ACTIVE.md`、主枝 `SAVE.md`、`CURRENT.md`、初始实体、地图层级、随机种子和前端数据。

失败信号：

- 未确认草案就开始游玩。
- AI 改写玩家锁定神格、尺度或内容边界。
- 前端地图成为事实来源，而不是从正式文件导出。

#### 片段 2：普通近景互动

玩家输入示例：

> 点击河谷城码头上的流浪祭司棋子，问他为什么要把断裂的铜环丢进河里。

必须验证：

- 只读取当前场景、目标角色状态、直接相关事件和玩家动作。
- 不扫描整份剧情、全世界历史或兄弟分支。
- 互动可以快速完成，并在必要时写入章节正文、事件节点或人物状态差异。
- 若对话触及旧誓约、秘密或不可逆后果，才沿指针补读对应事件和原文。
- 互动结束后由脚本更新事件、关系、编年史和前端棋子状态。

失败信号：

- 普通对话读取大量无关历史。
- 角色使用自己不知道的秘密回答。
- 小互动被强行扩写成不可控长篇剧情。

#### 片段 3：跨月或跨年编年史推进

玩家输入示例：

> 让世界自然运行一年，只在战争、死亡、政权更替、关键背叛或需要我决定时暂停；默认用客观简史展示。

必须验证：

- `advance_world.py` 按混合推进预设处理焦点、活跃和背景层。
- 天气、遭遇和大概率事件使用可复现随机日志。
- 普通背景事件进入客观简史骨架，不生成无用长篇场景。
- 遇到默认暂停项时停止推进，并给出当前事件、影响范围和玩家选择。
- 前端纵向时间线增量更新，只展示当前分支血统链。

失败信号：

- 一次性预写多年未来并当作正史。
- 死亡、战争或继位没有暂停。
- 时间线混入平级分支。

#### 片段 4：改变过去并创建子枝

玩家输入示例：

> 回到“王子在春潮祭前夜失踪”这个事件之前，让我以神谕阻止他离开王宫。

必须验证：

- 系统识别这是改变已发生历史，不覆盖原分支。
- `create_branch.py` 创建子枝 `SAVE.md`，记录 `parent_save`、`parent_branch_id`、`fork_event`、`inherit_until`。
- 子枝只继承父级截至分叉点的事件和状态。
- 子枝推进后的剧情、状态、地图、随机流、时间线和编年史与平级分支隔离。
- 原分支仍可查看，旧事件不被删除。

失败信号：

- 直接改写父分支历史。
- 子枝读取或继承父分支分叉后的事件。
- 两个平级分支随机日志或角色状态互相污染。

#### 片段 5：玩家手动修改 Markdown 后合并

玩家行为示例：

> 玩家手动打开人物卡，把祭司的公开身份从“流浪祭司”改成“被放逐的河神庙司铎”，并误改了一个事件 ID。

必须验证：

- 下一次继续游戏前先运行 `detect_manual_edits.py`。
- 系统把公开身份修改标记为中风险，把事件 ID 修改标记为高风险。
- Codex 显示影响范围：人物卡、相关事件、时间线、索引和可能需要重建的前端数据。
- 玩家可以接受身份修改、拒绝事件 ID 修改。
- `apply_manual_edits.py` 创建检查点，写入 `manual-edit-ledger.jsonl`，并重建索引、时间线、前端数据和验证报告。

失败信号：

- 手动改动被静默接受为正史。
- 高风险事件 ID 修改没有逐项确认。
- 派生文件反向覆盖事件节点或原始剧情。

### 16.2 验证记录模板

每次验证记录建议使用同一格式，后续可以直接拆成 Skill 测试样例：

```markdown
## 验证片段：<编号与名称>

- 日期：
- 当前世界/分支：
- 玩家输入：
- 本轮允许读取：
- 实际读取：
- 写入/更新文件：
- 生成事件 ID：
- 前端应变化：
- 是否触发暂停：
- 是否需要玩家确认：
- 通过/失败：
- 发现的问题：
- 需要修改的蓝图规则：
```

---

## 17. 推荐开发顺序

### 阶段 A：共同完成设计

- 用 3～5 个真实游玩片段验证命令和文件设计。

### 阶段 B：文本游戏核心

- 初始化 Skill 与世界目录。
- 完成剧情追加、实体状态、事件节点和最小检索路径。
- 完成天气随机、神权覆盖与随机日志。

### 阶段 C：长期记忆

- 完成检查点、上下文估算、继承包、恢复和一致性检查。
- 进行长达 50～100 轮的压力测试。

### 阶段 D：神之视界

- 导出面板数据。
- 实现地图图钉、时间轴和常驻状态看板。
- 再评估是否加入自动推进与写入控制。

### 阶段 E：打包与安装

- 精简 `SKILL.md`，补齐按需参考文件和前端资源。
- 校验 Skill 结构、触发描述和新环境安装体验。
- 提供准确的 `$be-a-god` 调用方式。

---

## 18. 当前暂定结论

- 游戏暂定名为《成为神》，Skill 名称暂定为 `be-a-god`。
- 新世界采用玩家填空并锁定关键字段、AI 补全其余内容的混合创世方式。
- 前端提供创世弹窗、来源对照和局部重新生成，玩家确认后才写入正式世界。
- 前端提供“温和 / 标准 / 不主动淡化 / 自定义”内容边界、一键淡化和逐项限制；该设置影响呈现方式与生成约束，但不覆盖系统安全边界。
- 地图采用世界/大陆、地区、城镇/场景三级缩放；剧情角色在不同层级保持可追踪。
- 前端第一版采用纸质地图默认主题，并提供神性星盘主题切换；两种主题共享同一套地图、棋子和事件数据。
- 角色棋子第一版使用抽象徽章与状态环，头像只作为后续增强，不作为必要资产。
- 默认采用自动混合叙事：宏观观察用神明全知、棋子交互用人物近景、大跨度推进用编年史。
- 以本地 Markdown 作为玩家可读的长期事实与剧情载体。
- 使用“分支树 + 事件图 + 原文指针”，不在第一版强依赖向量数据库。
- 每条世界线作为主故事树中的一个真实枝丫存在，并用本枝 `SAVE.md` 固定父级、分叉事件和继承边界。
- 子分支只沿父指针回溯，分叉后的状态、事件、地图、编年史和随机流与平级分支完全隔离。
- 随机程序必须可复现、可审计、可被玩家覆盖。
- 随机数字默认不进入剧情正文，玩家可从前端“神之骰”切换结果或审计模式。
- 第一版采用混合神权模式；正常干预计算代价，绝对神权始终保留。
- 死亡、文明覆灭和世界毁灭在当前枝丫内真实生效；玩家可用新神权事件挽回，或从事发前创建子分支。
- 推进方式允许逐步、定时、条件、角色跟随和授权自动推进混合存在。
- 自动推进默认在死亡、毁灭、战争、政权更替、关键背叛和玩家决策处暂停，玩家可逐项调整。
- 每个命名角色都能设为关注、普通或忽略；忽略不会冻结角色，只会延后其个人故事呈现。
- 角色以可移动棋子显示在地图，漫游者和剧情就绪者也始终可见。
- 世界编年史随正式事件自动增量记录；客观简史作为默认事实索引，史诗编年作为可重建展示文本。
- 玩家手动修改 Markdown 后，系统先检测差异和影响范围，经过玩家确认后才合并并重建派生数据。
- 即时互动默认只读取小型交互包，完成互动后再进行必要的机械结算。
- 机械、确定和规则随机工作封装为脚本；高语义创造由大语言模型负责。
- 用自身检查点与继承包对抗长对话遗忘，不依赖平台内部压缩时机。
- 前端首版支持创世草案、地图观察和交互指令入口；正式世界修改仍经 Codex 确认后落盘。
- 前端首版必须常驻纵向可滚动时间线，并与地图图钉、角色棋子、编年史和分支点双向联动。
- 核心机制已进入片段验证阶段，先用 5 个真实游玩片段检查创世、近景互动、编年推进、历史分支和手动编辑合并。
- 片段 1 已完成设计干跑，并据此补明确认前草案区 `setup/drafts/` 与主枝随机文件 `seed.json`、`random-log.jsonl`。
- 片段 2 已完成设计干跑，并据此补明确认运行临时目录 `runtime/`、前端动作请求、小型交互包和受控补读流程。
- 片段 3 已完成设计干跑，并据此补明确认批量推进运行记录、推进游标、暂停包和禁止预写未发生未来。
- 片段 4 已完成设计干跑，并据此补明确认分支草案、玩家确认、子枝首个神权修订事件和子枝随机种子派生。
- 片段 5 已完成设计干跑，并据此补明确认文件指纹基线、手动编辑报告、合并计划、事实修正事件和阻塞项处理。
- 已创建第一版正式 Skill 骨架 `be-a-god/`，包含 `SKILL.md`、核心参考文件、世界模板和两个基础脚本。
- 已补入 `init_world.py`，可从已确认创世草案生成第一局正式世界目录。
- 已补入 `make_interaction_packet.py`，可为点击角色、物体、地点或事件图钉生成最小互动包。
- 已补入 `settle_interaction.py`，可在确认后把互动结果写回事件、状态、编年史、时间线和前端数据。
- 已补入 `update_chronicle.py`、`build_timeline.py`、`export_dashboard.py`，可从事件和状态重建编年史、时间线和前端看板。
- 已补入 `build_indexes.py` 与 `validate_world.py`，可重建索引并检查当前世界结构、指针和派生文件。
- 已补入 `checkpoint.py` 与 `create_branch.py`，可创建当前枝丫检查点和确认后的子枝。
- 已补入 `switch_branch.py` 与 `resolve_branch_view.py`，可切换当前枝丫并只沿父指针解析继承链。
- 已补入 `resolve_random.py`、`set_attention.py`、`build_ignored_digest.py`，可处理分支隔离随机、角色关注状态和忽略角色摘要。
- 已补入 `advance_world.py`，可机械推进当前枝丫时间并记录推进运行。
- 已补入 `build_map_layers.py` 与 `update_map_state.py`，可导出地图层和角色棋子位置。
- 已补入 `render_chronicle_style.py` 与 `detect_manual_edits.py`，可生成史诗编年展示文本并检测手动文件修改。
- 已增强 `checkpoint.py` 的核心文件快照，并补入保守版 `apply_manual_edits.py`。
- 已补入 `assets/frontend-template/` 静态前端模板，包含地图、棋子、图钉、dashboard 和纵向时间线。
- 已补入 `smoke_test.py`，可在临时目录跑核心脚本链路自检。
- `apply_manual_edits.py` 已支持显式 `--restore-rejected`，可从 checkpoint 核心快照恢复被拒绝手改。
- 已补入 `validate_world_brief.py`，并让 `init_world.py` 默认拒绝未确认的创世草案。
- 第一开发目标是长期剧情不乱、换对话不断档，而不是堆叠大量模拟模块。

---

## 19. 版本讨论记录

### v0.1（2026-08-12）

- 收录最初五项需求。
- 补充核心循环、神权优先级、时间精度分层和世界自治。
- 确定混合历史结构、三层记忆、可复现随机与跨对话继承方案。
- 划分 MVP、前端边界、验收场景和待确认问题。

### v0.2（2026-08-12）

- 确认第一版采用混合神权模式。
- 加入玩家自定义的混合推进预设及自动暂停边界。
- 将角色棋子、漫游、剧情就绪提示和点击触发纳入第一版。
- 加入自动世界编年史及事件原文指针。
- 建立即时互动的最小上下文读取协议。
- 明确脚本、规则随机、大语言模型和约束式创造的职责边界。

### v0.3（2026-08-12）

- 确认采用“玩家指定与锁定 + 必要填空模板 + AI 补全”的混合创世方式。
- 加入前端分步创世弹窗、三态字段控制和生成来源对照。
- 增加创世草案、玩家确认、局部重新生成和正式世界初始化边界。
- 为创世输入、结构化参数和生成历史增加独立文件及验收场景。

### v0.4（2026-08-12）

- 确认采用世界/大陆、地区、城镇/场景三级语义缩放地图。
- 明确每级地图的信息密度、棋子聚合、剧情角色优先显示和交互范围。
- 增加分层坐标、跨地区路径、地图版本与无剧情读取缩放规则。
- 补充地图构建脚本、存档文件和三级地图验收场景。

### v0.5（2026-08-12）

- 确认使用神明全知、人物近景与编年史自动混合叙事。
- 明确地图操作、棋子交互、时间推进和重大事件的视角路由。
- 加入玩家单轮覆盖、长期叙事偏好和视角恢复规则。
- 强化视角与角色知识隔离，并补充相应验收场景。

### v0.6（2026-08-12）

- 确认每个世界线分支以主故事文件树中的独立枝丫和 `SAVE.md` 存在。
- 加入父级存档、分叉事件、固定继承边界和写时分离规则。
- 将剧情、状态、事件、地图、编年史、队列和随机流改为按枝丫隔离。
- 新增分支创建、解析、切换脚本及兄弟分支防串线验收场景。

### v0.7（2026-08-12）

- 确认剧情默认隐藏概率和骰点，并提供可展开的“神之骰”审计面板。
- 将可上下滚动的纵向世界时间线列为第一版前端必备核心区。
- 明确时间刻度、事件状态、地图棋子联动、分支隔离和长历史增量渲染规则。
- 新增时间线导出脚本、数据文件及前端联动验收场景。

### v0.8（2026-08-12）

- 确认角色死亡、文明覆灭和世界毁灭在当前分支内真实且永久。
- 取消重要角色默认剧情护甲，明确死亡后的状态、棋子、队列和编年处理。
- 加入当前分支复活/重建与事前创建新枝丫两种神权挽回方式。
- 增加世界终局状态及死亡、毁灭和复活的验收场景。

### v0.9（2026-08-12）

- 确认死亡、毁灭、战争、政权更替、关键背叛和玩家决策为默认自动暂停项。
- 加入角色“关注 / 普通 / 忽略”三态按钮及分支级状态存储。
- 被忽略角色继续演化，但个人剧情只记录事件骨架并折叠时间线节点。
- 玩家点击后按需生成忽略期间摘要，且查看不会自动取消忽略。

### v0.10（2026-08-12）

- 确认前端提供内容边界预设、逐项自定义和“一键淡化本段”按钮。
- 明确“温和 / 标准 / 不主动淡化 / 自定义”四档含义，其中“不主动淡化”不覆盖 Codex 或平台安全边界。
- 新增 `setup/content-profile.json`，用于保存内容限制、呈现强度和玩家绝对禁区。
- 补充内容边界相关的 MVP 范围和验收场景。

### v0.11（2026-08-12）

- 确认第一版地图美术采用纸质地图默认主题，并提供神性星盘主题切换。
- 确认角色棋子使用抽象徽章、阵营颜色、状态环和短标记，不默认依赖 AI 头像。
- 明确主题切换只改变前端展示，不改变地图坐标、地点 ID、事件指针或正式存档。
- 补充主题资产目录、MVP 范围和地图棋子验收场景。

### v0.12（2026-08-12）

- 确认编年史采用“客观简史默认 + 史诗编年可切换”的双轨风格。
- 明确客观简史是检索、恢复和时间线定位入口，史诗编年只是可重建展示文本。
- 新增 `render_chronicle_style.py`，用于按需从客观简史生成史诗编年。
- 补充编年史风格相关的 MVP 范围和验收场景。

### v0.13（2026-08-12）

- 确认玩家手动修改 Markdown 后不自动进入正史，必须先检测差异、展示影响范围并由玩家确认。
- 新增 `system/manual-edit-ledger.jsonl`，记录手动编辑的检测、接受、拒绝和合并历史。
- 新增 `detect_manual_edits.py` 与 `apply_manual_edits.py`，用于差异分析、风险分类、确认合并和派生数据重建。
- 将核心机制待确认问题清空，下一阶段转入真实游玩片段验证。

### v0.14（2026-08-12）

- 将下一阶段明确为真实游玩片段验证，而不是继续抽象追加机制。
- 补充 5 个验证片段：创世初始化、普通近景互动、跨年编年推进、改变过去创建子枝、手动修改 Markdown 后合并。
- 为每个片段定义玩家输入示例、必须验证项和失败信号。
- 增加统一验证记录模板，便于后续转成 Skill 测试样例。

### v0.15（2026-08-12）

- 完成片段 1“创世填空到正式世界初始化”的设计干跑验证。
- 新增独立验证记录 `验证记录-片段1-创世初始化.md`。
- 根据验证结果补充 `setup/drafts/` 草案区，避免确认前草案混入正式世界。
- 明确主枝随机目录包含 `seed.json` 与 `random-log.jsonl`，保证初始世界随机可复现。

### v0.16（2026-08-12）

- 完成片段 2“普通近景互动”的设计干跑验证。
- 新增独立验证记录 `验证记录-片段2-普通近景互动.md`。
- 补充当前分支 `runtime/action-requests/` 与 `runtime/interaction-packets/`，明确它们是临时运行资料，不是事实来源。
- 为小型交互包增加允许来源、禁止来源、来源预算、知识边界和受控补读申请字段。
- 新增 `settle_interaction.py`，将互动结果机械写回章节、事件、状态、编年史、时间线和前端数据。

### v0.17（2026-08-12）

- 完成片段 3“跨月或跨年编年史推进”的设计干跑验证。
- 新增独立验证记录 `验证记录-片段3-跨年编年推进.md`。
- 补充当前分支 `runtime/advance-runs/`，记录批量推进请求、时间片、推进游标、随机调用和暂停原因。
- 明确 `advance_world.py` 必须分片推进，遇到暂停条件立即停止，不预写剩余未来。
- 新增暂停包 `pause.md` 的字段要求，用于恢复推进和向玩家展示未决选择。

### v0.18（2026-08-12）

- 完成片段 4“改变过去并创建子枝”的设计干跑验证。
- 新增独立验证记录 `验证记录-片段4-改变过去创建子枝.md`。
- 补充当前分支 `runtime/branch-drafts/`，用于保存玩家确认前的历史修订分支草案。
- 明确 `create_branch.py` 必须先生成分支草案，玩家确认后才创建正式子枝。
- 明确子枝创建后必须写入第一条 `divine-revision` 神权修订事件，并派生独立随机种子。

### v0.19（2026-08-12）

- 完成片段 5“玩家手动修改 Markdown 后合并”的设计干跑验证。
- 新增独立验证记录 `验证记录-片段5-手动修改合并.md`。
- 补充 `system/file-manifest.json` 与 `build_file_manifest.py`，用于记录核心文件指纹、事实来源和派生文件清单。
- 明确手动编辑检测报告落在当前枝丫 `runtime/manual-edit-reports/<report_id>/`，并以 `report.md` 与 `merge-plan.json` 区分展示报告和可执行计划。
- 明确事实性手动修改接受后必须写入 `manual-correction` 或 `divine-revision` 事件，无法自洽的高风险项进入 `blocked_changes`，不得静默合并。

### v0.20（2026-08-12）

- 创建第一版正式 Skill 骨架 `be-a-god/`，保留在项目目录中，暂不安装到全局 Codex Skill 目录。
- 编写正式 `SKILL.md`，把启动、继续、互动、推进、分支、手动编辑和上下文节省规则压缩为入口协议。
- 拆出 `references/storage-contract.md`、`references/game-master-protocol.md`、`references/frontend-contract.md`、`references/script-catalog.md`、`references/validation-scenarios.md`。
- 增加 `assets/world-template/` 的创世草案、存档和内容边界模板。
- 增加并验证 `scripts/build_file_manifest.py` 与 `scripts/validate_world_structure.py` 两个基础脚本。

### v0.21（2026-08-12）

- 新增 `scripts/init_world.py`，用于从玩家确认后的创世草案创建正式世界目录。
- `init_world.py` 默认拒绝写入，必须显式提供 `--confirmed`；支持 `--dry-run` 预览创建计划。
- 初始化内容包括世界元数据、主枝 `SAVE.md`、初始事件、客观编年、史诗编年、随机种子、地图空结构、索引、前端数据、系统日志和文件指纹清单。
- 更新正式 `SKILL.md`，在“Start a world”流程中明确调用 `init_world.py`。

### v0.22（2026-08-12）

- 新增 `scripts/make_interaction_packet.py`，用于从当前世界和当前枝丫生成最小近景互动包。
- 互动包默认只包含 `ACTIVE.md`、当前枝丫 `SAVE.md`、`CURRENT.md`、`PLAYER.md`、dashboard 数据和目标卡片，不读取完整章节或平级分支。
- 互动包包含 `allowed_sources`、`forbidden_sources`、`source_budget`、`knowledge_boundary` 和 `followup_source_request`，用于控制 Codex 是否需要申请补读历史。
- 更新正式 `SKILL.md` 与 `game-master-protocol.md`，明确点击角色、物体、地点、棋子或图钉时先调用该脚本，再快速互动。

### v0.23（2026-08-12）

- 新增 `scripts/settle_interaction.py`，用于在玩家确认后把近景互动结果写入正式世界文件。
- `settle_interaction.py` 默认拒绝写入，必须显式提供 `--confirmed`；支持 `--dry-run` 预览将写入的事件、编年、时间线、dashboard 和 manifest。
- 结算写入内容包括新事件节点、`CURRENT.md`、当前枝丫 `SAVE.md`、客观编年、纵向时间线、dashboard 图钉、可选人物状态追加、轮次流水和文件指纹清单。
- 更新正式 `SKILL.md` 与 `game-master-protocol.md`，明确互动叙事完成后由该脚本进行机械结算。

### v0.24（2026-08-12）

- 新增 `scripts/update_chronicle.py`，可从当前枝丫事件节点重建客观编年史。
- 新增 `scripts/build_timeline.py`，可从当前枝丫事件节点重建 `dashboard/timeline.json`。
- 新增 `scripts/export_dashboard.py`，可从当前存档、人物状态和事件节点导出轻量前端看板数据。
- 这些脚本用于恢复派生文件，确保索引、时间线和前端数据可从核心事实重新生成。

### v0.25（2026-08-12）

- 新增 `scripts/build_indexes.py`，从当前枝丫人物状态卡和事件节点生成实体索引与事件索引。
- 新增 `scripts/validate_world.py`，检查世界根文件、当前枝丫结构、`ACTIVE.md` 与 `SAVE.md` 指针、事件 ID、manifest 和派生 JSON。
- 更新脚本目录，形成“事件/状态 → 编年史/时间线/dashboard/索引 → 验证”的基础恢复链路。

### v0.26（2026-08-12）

- 新增 `scripts/checkpoint.py`，可为当前枝丫保存 `SAVE.md`、`CURRENT.md`、时间线、dashboard 和文件指纹清单。
- 新增 `scripts/create_branch.py`，用于在玩家确认后从当前枝丫创建子枝，写入子枝 `SAVE.md`、`CURRENT.md`、首个 `divine-revision` 事件、编年史和独立随机种子。
- `create_branch.py` 默认拒绝写入，必须显式提供 `--confirmed`；支持 `--dry-run` 和 `--no-switch`。
- 子枝默认嵌套在当前枝丫 `branches/<branch-id>/` 下，并可自动切换 `ACTIVE.md`。

### v0.27（2026-08-12）

- 新增 `scripts/switch_branch.py`，可把 `ACTIVE.md` 切换到指定枝丫。
- 新增 `scripts/resolve_branch_view.py`，可输出当前枝丫及其父级继承链。
- `resolve_branch_view.py` 只读取当前枝丫和 `parent_save` 指针链，不扫描平级分支，符合节省上下文和防串线要求。

### v0.28（2026-08-12）

- 新增 `scripts/resolve_random.py`，按当前枝丫种子和随机日志位置生成可复现随机结果，并记录玩家覆盖。
- 新增 `scripts/set_attention.py`，支持把角色设为 `followed`、`normal` 或 `ignored`，并同步轻量 dashboard 状态。
- 新增 `scripts/build_ignored_digest.py`，可为被忽略角色按需生成事件骨架摘要，且不会自动取消忽略状态。

### v0.29（2026-08-12）

- 新增 `scripts/advance_world.py`，用于机械推进当前枝丫时间、记录 `runtime/advance-runs/<run_id>/run.json`，并同步 `SAVE.md`、`CURRENT.md` 和 dashboard 时间。
- `advance_world.py` 默认拒绝写入，必须显式提供 `--confirmed`；支持 `--dry-run`。
- 修正 `init_world.py` 初始化空 JSONL 文件时写入空行的问题，使随机日志等文件初始状态真正为空。
- 当前推进脚本只做机械推进；战争、死亡、政权更替等高语义事件仍由模型生成后通过事件结算写入。

### v0.30（2026-08-12）

- 新增 `scripts/build_map_layers.py`，从 `base/maps/hierarchy.json` 与 `base/maps/coordinates.json` 导出前端地图层数据。
- 新增 `scripts/update_map_state.py`，从当前枝丫人物状态卡读取位置，并根据地图坐标更新 dashboard 棋子。
- 地图导出不包含剧情正文，保持“地图缩放不读取故事”的上下文节省原则。

### v0.31（2026-08-12）

- 新增 `scripts/render_chronicle_style.py`，从客观编年史生成可重建的史诗编年展示文本。
- 新增 `scripts/detect_manual_edits.py`，根据 `system/file-manifest.json` 检测新增、修改、删除文件，并输出手动编辑报告与合并计划。
- `detect_manual_edits.py` 只做检测，不自动接受手动修改为正史。
- 暂缓实现 `apply_manual_edits.py`：当前 manifest 只有哈希和权限分类，没有旧文件内容；若玩家拒绝某项修改，系统需要明确的恢复来源策略。

### v0.32（2026-08-12）

- 增强 `scripts/checkpoint.py`，默认把 manifest 中 `authority=core` 的文件复制到 `core-snapshot/`，并在 `checkpoint.json` 记录恢复映射。
- 新增保守版 `scripts/apply_manual_edits.py`，只把玩家明确接受的手动修改纳入 manifest，并写入 `manual-edit-ledger.jsonl`。
- `apply_manual_edits.py` 会在应用前自动创建 checkpoint；拒绝项只记录、不自动恢复，因此仍会在后续检测中出现，直到玩家手动改回或后续启用快照恢复策略。
- 高风险接受必须显式提供 `--allow-high-risk`，避免误把事件 ID、随机日志、父指针等结构改动纳入正史。

### v0.33（2026-08-12）

- 新增 `be-a-god/assets/frontend-template/` 静态前端模板。
- 模板包含 `index.html`、`styles.css`、`app.js`、`sample-dashboard.json` 和 `sample-timeline.json`。
- 首版模板展示常驻世界状态、地图棋子、事件图钉、详情面板、神权动作按钮和纵向可滚动时间线。
- 模板只读取前端导出数据和样例数据，不读取剧情正文；正式落盘仍由 Codex 与脚本执行。

### v0.34（2026-08-12）

- 新增 `scripts/smoke_test.py`，在临时目录中运行核心端到端脚本链路。
- smoke test 覆盖世界初始化、互动包、互动结算、编年史/时间线/dashboard/索引重建、验证器、检查点、分支、分支视图、随机、关注/忽略、手改检测与接受、前端模板样例文件。
- 更新脚本目录，标记 MVP 脚本集已实现；后续新增脚本应对应新的稳定机制。

### v0.35（2026-08-12）

- 增强 `scripts/apply_manual_edits.py`，新增显式 `--restore-rejected` 选项。
- 默认行为仍是记录拒绝项但不恢复；只有玩家明确要求恢复时，才从 checkpoint 的 `core-snapshot/` 复制旧文件内容。
- 恢复前会检查快照文件哈希是否匹配旧 manifest；找不到匹配快照时失败，不猜测旧内容。
- `scripts/smoke_test.py` 已覆盖拒绝项快照恢复路径。

### v0.36（2026-08-12）

- 新增 `scripts/validate_world_brief.py`，用于检查 `WORLD-BRIEF.md` 是否已确认，以及关键创世字段是否缺失。
- `scripts/init_world.py` 默认拒绝从未包含 `Status: confirmed` 的草案创建正式世界；如确需覆盖，必须显式传 `--allow-unconfirmed-brief`。
- 更新正式 `SKILL.md` 的创世流程，在初始化正式世界前先运行创世草案校验。

### v0.37（2026-08-12）

- 新增 `scripts/check_install_ready.py`，用于在真实安装前做只读就绪检查。
- 检查范围包括 `SKILL.md` 元数据、`agents/openai.yaml`、核心目录与模板、核心引用文件、Python 脚本编译、UTF-8 编码，以及全局 Skill 目标目录是否存在同名冲突。
- 支持 `--run-smoke` 选项，把端到端 smoke test 纳入安装前验收；默认不复制、不覆盖、不安装任何文件。
- 更新脚本目录，把安装就绪检查作为“从项目本地 Skill 包过渡到可安装 Skill”的安全关口。

### v0.38（2026-08-12）

- 增强 `assets/frontend-template/`，新增 dashboard 与 timeline JSON 文件选择入口。
- 前端模板在被本地服务托管时优先读取同目录的 `dashboard.json` 和 `timeline.json`；若不存在则回退到样例数据。
- 玩家也可以直接选择 `export_dashboard.py` 与 `build_timeline.py` 导出的 JSON 文件进行观察，不需要修改前端代码。
- 手动载入 JSON 只改变前端展示，不写入世界文件；正式世界状态仍由 Codex 与脚本确认后落盘。

### v0.39（2026-08-12）

- 新增 `scripts/prepare_frontend.py`，用于把静态前端模板复制到指定世界的 `<world>/frontend`。
- 发布脚本会把世界导出的 `dashboard/data.json` 与 `dashboard/timeline.json` 复制为前端同目录的 `dashboard.json` 与 `timeline.json`，使面板可直接载入真实世界数据。
- 脚本默认只预览；真实写入必须显式传 `--confirmed`，且输出目录必须位于该世界目录内部。
- 已有前端文件默认不覆盖；如需刷新同一前端目录，必须显式传 `--overwrite`。

### v0.40（2026-08-12）

- 新增 `scripts/create_world_brief.py`，用于把玩家在创世填空界面或对话中给出的字段生成 `WORLD-BRIEF.md` 草案。
- 草案默认写入 `Status: draft`，不会触发正式世界初始化；玩家确认后才可改为 `Status: confirmed` 并交给 `init_world.py`。
- 支持玩家锁定字段、可润色字段、AI 补全字段、内容边界和玩家备注；未填写部分保留为空或交给 AI 补全。
- `scripts/smoke_test.py` 已改为使用该脚本生成开局草案，再模拟确认和初始化。

### v0.41（2026-08-12）

- 增强静态前端模板，新增“创世填空”弹窗。
- 玩家可以填写世界 ID、世界前提、起始时代、起始地区、神明身份、叙事基调、绝对禁区、类型参考、初始文化、内容边界和备注。
- 前端只生成并下载 `WORLD-BRIEF.md` 草案，默认保留 `Status: draft`；不会直接创建正式世界，也不会写入任何本地世界文件。
- `scripts/smoke_test.py` 已加入创世弹窗关键标记检查，防止后续改前端时误删该入口。

### v0.42（2026-08-12）

- 新增 `scripts/create_handoff.py`，用于在上下文变长或准备换新对话时生成枝丫内继承存档。
- handoff 默认写入当前枝丫 `runtime/context-handoffs/<handoff_id>/HANDOFF.md` 与 `handoff.json`，包含当前世界、枝丫、存档指针、当前状态、继承关系、开放事项、最近编年史、最近事件节点、随机日志位置和下一轮 first-read 文件清单。
- handoff 是支持文件，不替代 `SAVE.md`、事件节点、实体卡、随机日志或原始剧情；需要精确回忆时仍按源指针回溯。
- 新世界和新枝丫默认创建 `runtime/context-handoffs/` 目录；写入 handoff 后刷新 `system/file-manifest.json`。
- `scripts/smoke_test.py` 已覆盖 handoff 创建、文件存在性、first-read 指针和最近事件上下文。

### v0.43（2026-08-13）

- 新增 `scripts/check_context_pressure.py`，用于估算当前枝丫是否应该生成上下文继承存档。
- 检查指标包括最小续玩上下文字符数、核心历史字符数、事件数量、编年史条目、随机日志条目和最近 handoff。
- 默认状态分为 `keep-going`、`suggest-handoff`、`urgent-handoff`；阈值可通过参数覆盖。
- 正式 `SKILL.md` 已把上下文压力检查接入“Create a context handoff”流程：先检查压力，再在建议或紧急状态下生成 handoff。
- `scripts/smoke_test.py` 已覆盖首次无 handoff 时建议生成，以及生成 handoff 后恢复 `keep-going` 的状态变化。

### v0.44（2026-08-13）

- 增强 `scripts/resolve_random.py` 的天气处理：随机天气和玩家天气覆盖都会写入当前枝丫 `random/random-log.jsonl`。
- 当 `kind=weather` 且未显式跳过时，脚本会同步更新 `dashboard/data.json` 的常驻天气字段和最近随机记录。
- 随机日志写入后默认刷新 `system/file-manifest.json`，避免后续手动编辑检测把正常随机日志误判为玩家手改。
- `scripts/smoke_test.py` 已覆盖玩家用 `--override` 改天气，并确认前端 dashboard 天气同步。

### v0.45（2026-08-13）

- 新增 `scripts/queue_event.py`，用于把已知未来触发器、旅行抵达、剧情就绪点、高优先级风险或灾害预警写入当前枝丫 `queues/events.jsonl`。
- 增强 `scripts/advance_world.py`：推进时间时会检查当前枝丫事件队列，若请求跨度内存在需要暂停的事件，则只推进到该事件触发时间。
- 自动暂停会写入 `runtime/advance-runs/<run_id>/pause.md`，把队列项标记为 `due`，并同步 `SAVE.md`、`CURRENT.md` 与 dashboard 的 unresolved choices。
- 队列项不会自动变成正史事件；真正的事件节点仍需由模型叙事和玩家确认后的结算脚本写入。
- `scripts/smoke_test.py` 已覆盖“排队洪水警报 → 推进 30 天 → 在第 5 天暂停 → 写 pause.md → 队列标记 due”的链路。

### v0.46（2026-08-13）

- 新增 `scripts/settle_queued_event.py`，用于把已经 `due` 的队列事件结算为正式历史事件。
- 队列结算会写事件节点、追加客观编年史、更新 `SAVE.md` 与 `CURRENT.md`、把队列项标记为 `settled`、移除 dashboard unresolved choices、追加 `system/turn-ledger.jsonl` 并刷新 manifest。
- 默认只能结算 `due` 队列项；若要提前结算，必须显式传 `--allow-not-due`，并仍需要 `--confirmed`。
- 正式 `SKILL.md` 已在时间推进流程中加入：队列事件到期后先由模型叙事和玩家确认，再调用队列结算脚本落盘。
- `scripts/smoke_test.py` 已覆盖“到期洪水警报 → 结算为正式事件 → 清除暂停 → 队列 settled → dashboard 待决项消失”的链路。

### v0.47（2026-08-13）

- 增强 `scripts/validate_world.py`，从基础结构检查扩展为关键数据一致性检查。
- 新增检查范围包括：当前枝丫 `queues/events.jsonl` 队列状态、重复 queue ID、settled 队列事件文件指针、随机种子和随机日志模式、handoff 的 `HANDOFF.md` 与 `first_read` 指针、已发布前端目录 JSON、manifest 缺失指针。
- 验证器现在能发现“队列已结算但事件文件不存在”“handoff 要求读取的文件不存在”“随机日志模式非法”等更接近真实游玩风险的问题。
- 更新脚本目录，把 `validate_world.py` 标记为世界结构与关键派生数据的综合验证入口。

### v0.48（2026-08-13）

- 增强 `scripts/check_install_ready.py`，新增脚本目录一致性检查。
- 安装前检查现在会比对 `references/script-catalog.md` 中登记的 `scripts/*.py` 与真实 `scripts/` 目录，发现未登记脚本或文档中引用了不存在脚本时直接失败。
- 该检查用于防止后续持续补脚本时出现“代码存在但 Skill 不知道如何调用”或“文档指向已删除脚本”的问题。

### v0.49（2026-08-13）

- 新增 `scripts/set_content_profile.py`，用于设置或更新世界的内容边界、呈现强度、逐项 topic 策略和玩家绝对禁区。
- 支持 `gentle`、`standard`、`unsoftened`、`custom` 四类预设，并支持 topic 级 `allow`、`summary`、`soften`、`avoid`。
- 支持创建一次性 `runtime/soften-requests/<id>.json` 淡化请求；该请求只改变呈现方式，不允许改写事件节点、状态变化、随机日志、队列、死亡、损伤或编年史事实。
- 新世界和新枝丫默认创建 `runtime/soften-requests/` 目录；`PLAYER.md` 会同步一份紧凑内容边界摘要，供小型交互包读取。
- `scripts/validate_world.py` 已检查 `setup/content-profile.json` 的 topic 模式、绝对禁区列表和 `facts_remain_intact_when_softened=true`。
- `scripts/smoke_test.py` 已覆盖内容边界设置、绝对禁区、一次性淡化请求和“淡化不改变事件数量”的边界。

### v0.50（2026-08-13）

- 增强静态前端创世弹窗，把内容边界从自由输入改为 `standard`、`gentle`、`unsoftened`、`custom` 预设选择。
- 新增“内容禁区”多行字段，生成 `WORLD-BRIEF.md` 草案时写入 `absolute bans`，与 `set_content_profile.py` 的玩家绝对禁区语义对齐。
- 更新前端合同，要求创世向导暴露内容边界预设和绝对禁区字段。
- `scripts/smoke_test.py` 已加入前端内容边界字段与草案输出标记检查。

### v0.51（2026-08-13）

- 新增 `scripts/create_entity.py`，把角色、地点、势力、物品、物体的机械化落盘封装为脚本：ID、文件名、实体卡、地点坐标、地图层、索引、棋子面板与 manifest 都由脚本处理。
- 明确分工：脚本不生成国王性格、政治动机、秘密、文化意义等高随机/高语义内容；这些仍由玩家或大语言模型生成，再作为 `--traits-json` 写入实体卡。
- 地点使用 `state/locations/` 与 `base/maps/hierarchy.json` / `coordinates.json`；角色等可移动对象使用 `state/entities/` 并作为前端棋子展示。
- `export_dashboard.py` 与 `update_map_state.py` 已支持实体 `kind`、`attention`、直接 `x/y/level` 和通过地点坐标落图两种方式。
- `scripts/smoke_test.py` 已改为通过 `create_entity.py` 创建河湾集市和米拉，验证“创世后创建地点 → 创建角色棋子 → 继续交互/推进/队列/前端”的链路。

### v0.52（2026-08-13）

- 修复交互包目标解析规则：点击目标时先按 Markdown 卡片中的 `- id:` 精确匹配，再进行文件名或正文模糊兜底。
- 该规则避免角色卡里的 `location: LOC-001` 抢走本应指向 `LOC-001` 地点卡的点击。
- `scripts/make_interaction_packet.py` 的目标类型枚举已加入 `location`，并保留 `place` 兼容自然语言叫法。
- `scripts/smoke_test.py` 新增地点点击验证，确认 `LOC-001` 会解析到 `story/main/state/locations/LOC-001-river-market.md`。

### v0.53（2026-08-13）

- 静态前端接入 `map-layers.json`：启动时优先读取世界导出的地图层，缺失时回退到 `sample-map-layers.json` 或内置样例。
- 前端新增手动载入 map layers 文件入口，地图层节点可作为地点被点击，并生成 `target_kind: location` 的动作建议。
- `scripts/prepare_frontend.py` 现在会把 `dashboard/map-layers.json` 复制为世界前端目录下的 `map-layers.json`。
- `scripts/validate_world.py` 已把 `dashboard/map-layers.json` 和前端 `map-layers.json` 纳入 JSON 检查范围。
- `scripts/smoke_test.py` 已覆盖地图层样例文件、前端模板标记、准备后的 `map-layers.json` 文件和 JSON 解析。

### v0.54（2026-08-13）

- `scripts/build_map_layers.py` 会读取当前枝丫的 `state/locations/*.md`，并把地点卡 source 指针合并进 `dashboard/map-layers.json`。
- 这样前端地图节点不只显示地理点位，还能指向对应 Markdown 位置，但仍不主动读取剧情正文。
- 前端层级调整为：地理节点最低、事件图钉居中、角色棋子最高，保证玩家漫游时能优先看到人物棋子。
- `scripts/smoke_test.py` 已断言 `LOC-001` 地点节点包含 `story/main/state/locations/LOC-001-river-market.md` source 指针。

### v0.55（2026-08-13）

- `scripts/build_indexes.py` 新增 `indexes/locations.json`，让地点卡可以像角色与事件一样被轻量检索。
- 地点索引包含 `id`、`name`、`kind`、`level`、父地点、状态、坐标和 source 指针。
- `scripts/validate_world.py` 已把 `indexes/locations.json` 纳入派生 JSON 检查。
- `scripts/smoke_test.py` 已断言 `LOC-001` 会进入地点索引并保留地点卡 source。

### v0.56（2026-08-13）

- 新增 `scripts/move_entity.py`，用于把角色/物体等可见棋子移动到地点、直接坐标或地图层级，并更新状态。
- 移动脚本只处理机械状态：`location`、`x/y`、`level`、`status`、移动备注、索引、地图棋子和 manifest；“为什么移动”仍由玩家或模型决定。
- `create_entity.py` 与 `move_entity.py` 的 `--json` 输出已改为纯 JSON，内部刷新脚本日志不再污染 stdout。
- `scripts/smoke_test.py` 已覆盖“米拉移动到新坐标 → dashboard 棋子位置和状态更新”的链路。

### v0.57（2026-08-13）

- 静态前端动作区新增“忽略此人”和“关注此人”按钮，对应玩家要求的“忽略一些人的事情，直到主动点击才看”。
- 这两个按钮不生成普通交互包，而是给出 `scripts/set_attention.py --state ignored/followed` 的机械命令建议。
- 前端合同已明确：忽略/关注属于注意力状态切换，不应读取或改写角色剧情正文。
- `scripts/smoke_test.py` 已检查前端模板包含 ignore/follow 入口和 `set_attention.py` 动作建议。

### v0.58（2026-08-13）

- 新增 `scripts/wander_entities.py`，用于执行例行漫游 tick：从当前枝丫随机种子、世界时间和实体 ID 生成目的地，或接受玩家 `ENTITY=LOCATION` 覆盖。
- 漫游脚本默认包含 ignored 角色，保证被玩家忽略的人仍会移动和生活，只是故事不主动展开。
- 每个漫游结果都会追加 `random/random-log.jsonl`，`kind: wander`，并区分 `mode: random` 与 `mode: override`。
- 漫游只改实体卡位置/状态并刷新 dashboard 棋子，不创建事件节点或编年史；如果某次移动变成剧情事件，再由模型和结算脚本提升为正式事件。
- `scripts/smoke_test.py` 已覆盖“玩家覆盖米拉目的地到 LOC-002 → dashboard 棋子移动 → random log 记录 wander override”的链路。

### v0.59（2026-08-13）

- 修正静态前端动作区：`推进时间` 改为全局动作，不再要求玩家先点击棋子或事件。
- 新增 `改天气` 全局按钮，生成 `scripts/resolve_random.py --kind weather --override <weather>` 命令建议，体现玩家随时覆盖天气的神权。
- 前端合同已明确：推进时间与天气覆盖是全局动作，不能依赖当前选中目标。
- `scripts/smoke_test.py` 已检查前端模板包含 `weather-override`、`emitGlobalAction`、`advance_world.py` 和 `resolve_random.py` 标记。

### v0.60（2026-08-13）

- `scripts/advance_world.py` 新增 `--wander`、`--wander-limit` 和 `--wander-exclude-ignored`，允许非暂停时间推进后自动执行一次漫游 tick。
- 如果推进命中队列暂停，`--wander` 会跳过并在 `run.json` 里记录原因，避免普通漫游掩盖重大事件。
- `advance_world.py` 的运行 ID 从秒级改为微秒级，修复同一秒内连续推进导致 `advance-runs/ADV-*` 目录冲突的问题。
- `scripts/smoke_test.py` 已增强子进程失败报告，会输出失败命令、stdout 和 stderr，便于定位真实错误。
- `scripts/smoke_test.py` 已覆盖“队列结算后推进 1 天并触发 `--wander` → run.json 记录 wandering report → dashboard 时间保持一致 → random log 写入 advance-scoped wander”的链路。

### v0.61（2026-08-13）

- 新增 `scripts/draft_branch.py`，用于在当前枝丫 `runtime/branch-drafts/<draft_id>/` 下创建非正史的分支草案。
- 草案包含父枝丫、父存档、fork event、继承边界、拟创建枝丫路径、首次修改摘要和随机种子派生预览。
- `scripts/create_branch.py` 新增 `--draft-id`，可从草案创建正式子枝丫；创建成功后把草案标记为 `status: consumed` 并记录 `created_branch_path`。
- `create_branch.py` 创建分支后会刷新 manifest，避免新枝丫和草案文件不被后续手动修改检测覆盖。
- `scripts/smoke_test.py` 已覆盖“草案创建 → 草案消费 → STORY-TREE 记录子枝丫 → resolve_branch_view 回溯父链 → 切回主枝丫”的完整链路。

### v0.62（2026-08-13）

- `scripts/validate_world.py` 新增枝丫结构检查：活动子枝丫必须出现在 `STORY-TREE.md`，活动枝丫的 `parent_save` 必须存在。
- 验证器会审计所有 `runtime/branch-drafts/*/draft.json`：检查 schema、`draft/consumed` 状态、父枝丫路径、已消费草案对应的正式分支和 STORY-TREE 记录。
- 该检查只在显式运行验证器时进行，不改变正常游玩中“不扫描平级兄弟分支”的读取策略。
- `scripts/smoke_test.py` 已通过新验证器，覆盖已消费草案与正式子枝丫记录的一致性。

### v0.63（2026-08-13）

- `scripts/create_branch.py` 在创建并切换到子枝丫后，会立即重建当前活动枝丫的 `dashboard/timeline.json`、`dashboard/data.json`、`indexes/*.json`、`dashboard/map-layers.json` 和地图棋子状态。
- 如果使用 `--no-switch`，不会重建派生看板，避免把父枝丫的当前观察面意外切到子枝丫。
- `scripts/smoke_test.py` 已覆盖“创建子枝丫后 active child 立即通过 `validate_world.py`，且 dashboard branch_id 为子枝丫”的链路。

### v0.64（2026-08-13）

- `scripts/create_handoff.py` 现在同时写入 `HANDOFF.md` 和中文别名 `存档.md`，二者内容一致，满足玩家“跨对话继承存档以存档.md形式存在”的使用习惯。
- `handoff.json` 保持机器可读格式不变，`HANDOFF.md` 保持英文兼容入口。
- `scripts/validate_world.py` 已要求每个 handoff 同时存在 `HANDOFF.md`、`存档.md` 和 `handoff.json`。
- `scripts/smoke_test.py` 已覆盖 `存档.md` 存在性以及与 `HANDOFF.md` 内容一致。

### v0.65（2026-08-13）

- 新增 `scripts/create_action_request.py`，把前端按钮、地图点击和玩家快速命令先写入当前枝丫 `runtime/action-requests/<request_id>/`。
- 动作请求只属于 support 文件：记录 action、target、intent、建议后续脚本和 `canonical_effect: none`，不会直接改变正史、状态、随机日志、队列、编年史、dashboard 或 timeline。
- 静态前端模板的动作建议已改为调用 `create_action_request.py`；真正的互动包、推进时间、天气覆盖、忽略/关注或分支草案仍由后续脚本在确认后执行。
- `scripts/validate_world.py` 已检查 action request 的 schema、状态、Markdown 伴随文件和非正史策略。
- `scripts/smoke_test.py` 已覆盖“神力干预请求 → 建议互动包”和“天气覆盖请求 → 建议随机解析器”的链路。

### v0.66（2026-08-13）

- 新增 `scripts/update_action_request.py`，用于把动作请求标记为 `accepted`、`executed` 或 `cancelled`。
- 生命周期更新只修改该请求目录内的 support 文件和 manifest，不会执行建议命令，也不会改变正史。
- 动作请求 Markdown 现在会记录 lifecycle 条目，方便回看玩家曾经确认、执行或取消过哪些神权动作。
- `scripts/smoke_test.py` 已覆盖动作请求从 `requested` 到 `accepted` 的状态转换。

### v0.67（2026-08-13）

- 新增 `scripts/list_action_requests.py`，用于按当前枝丫列出动作请求，可用 `--pending` 只看 `requested` 与 `accepted`。
- `scripts/export_dashboard.py` 现在导出 `pending_action_requests`，只包含请求摘要和 source 指针，不读取剧情正文。
- 静态前端模板右侧新增“待处理动作”列表，点击后在详情区显示动作请求摘要。
- `scripts/smoke_test.py` 已覆盖动作请求列表和 dashboard pending request 导出。

### v0.68（2026-08-13）

- 修正蓝图旧路径，把 `runtime/action-requests.jsonl` 统一为当前实现的 `runtime/action-requests/<request_id>/` 目录制。
- `scripts/make_interaction_packet.py` 新增 `--request-id`，可把动作请求挂接到近景交互包。
- 挂接动作请求时会校验请求 schema 与 target ID，避免一个前端动作误接到另一个目标。
- `scripts/create_action_request.py` 生成 observe/speak/intervene 建议命令时会自动带上 `--request-id`。
- `scripts/smoke_test.py` 已覆盖动作请求 ID 贯穿到交互包的链路。

### v0.69（2026-08-13）

- 新增 `scripts/read_source_packet.py`，用于按明确 source 指针读取短片段。
- source packet 只允许读取世界根白名单文件、当前枝丫及其父链内文件，阻止越界路径和平级分支串线。
- 可通过 `--from-packet` 读取交互包中的 `context_policy.allowed_sources`，把“必要时补读旧事”变成可审计的机械动作。
- `scripts/smoke_test.py` 已覆盖从 `IP-SMOKE` 交互包读取 allowed sources，并确认越界 source 被阻断。

### v0.70（2026-08-13）

- 新增 `scripts/assess_divine_action.py`，用于混合神权模式下预览干预代价。
- 评估结果写入当前枝丫 `runtime/divine-assessments/<assessment_id>/`，包含 JSON 与 Markdown 两份 support 文件。
- 评估只计算 action、scale、irreversibility、visibility 和 target count 的普通代价，不执行神权动作，不改正史。
- 当玩家声明绝对神权时，评估记录 `status: absolute-authorized`，后续实际效果仍必须通过对应结算脚本写入。
- 新世界、新枝丫和验证器已纳入 `runtime/divine-assessments/`；`scripts/smoke_test.py` 已覆盖普通超限与绝对神权两条路径。

### v0.71（2026-08-13）

- 静态前端模板新增纸质地图 / 神性星盘主题切换按钮。
- 主题状态只保存在前端本地展示层，不改变地图坐标、地点 ID、事件指针、dashboard、timeline 或任何世界文件。
- `assets/frontend-template/styles.css` 新增 `body[data-theme="star-chart"]` 样式；`app.js` 新增 `applyTheme` / `toggleTheme`。
- `scripts/smoke_test.py` 已覆盖前端主题按钮、JS 主题函数和 CSS 主题标记。
### v0.72（2026-08-13）

- 新增 `setup/advance-profile.json`，用于保存玩家自定义推进预设；它是 support 配置，不是剧情正史。
- 新增 `scripts/set_advance_profile.py`，支持创建或更新 `step`、`hybrid`、`chronicle`、`event-priority` 等推进预设，并可设置默认预设。
- 增强 `scripts/advance_world.py`：支持 `--preset <id>`，在未提供 `--days` 时使用默认或指定预设；临时命令参数仍高于长期预设。
- 新增 `advance_world.py --until-next-queue` 与 `event-watch.until_next_queue`，用于把推进跨度解析到当前枝丫下一条队列暂停/高优先级事件。
- 推进运行记录 `run.json` 现在会记录 `advance_profile` 来源、预设 ID、预设内容和 CLI 覆盖项，便于审计“这次为什么这样推进”。
- `export_dashboard.py` 会导出轻量 `dashboard.advance_profile`，前端推进按钮可优先使用默认推进预设生成动作请求。
- `validate_world.py`、`check_install_ready.py` 和 `smoke_test.py` 已纳入推进预设配置、脚本目录和预设执行链路检查。

### v0.73（2026-08-13）

- 新增 `scripts/build_event_graph.py`，从当前活动枝丫事件节点重建 `indexes/event-graph.json`。
- 事件图索引包含事件节点、cause/effect 链接、actor/location/tag 反向索引和 unresolved refs，用于按因果和参与者快速定位旧事件。
- `build_indexes.py` 现在会同步输出 `event-graph.json`，新世界初始化也会生成初始事件图。
- `settle_interaction.py` 与 `settle_queued_event.py` 支持把 result JSON 中的 `actors`、`location`、`causes`、`effects`、`tags` 写入事件节点元数据。
- `validate_world.py` 已检查 event graph 的 schema、分支一致性、事件节点覆盖和链接基本结构。
- `SKILL.md` 与主持协议已要求：先用事件图找候选旧事件，再用 source pointer 精确补读，避免读取大段历史。
- `switch_branch.py` 现在切换 ACTIVE 后默认重建 dashboard、timeline、indexes、event graph、map layers 和 manifest，避免派生数据停留在旧枝丫。

### v0.74（2026-08-13）

- 新增结构化世界规则机制：`setup/world-rules.json` 保存玩家确认的世界规则和不可更改事实，`CANON.md` 同步展示 active 规则摘要。
- 新增 `scripts/set_world_rule.py`，支持创建、更新、替换、撤销或标记规则，并记录 scope、target、effective_time、tags 和分支来源。
- `make_interaction_packet.py` 会把紧凑的 `world_rules.active` 放入近景交互包，使 AI 在快速交互中遵守锁定事实，不必读取完整 CANON。
- `export_dashboard.py` 会导出 `dashboard.world_rules.active`；前端模板新增“当前规则”看板和“锁定规则”按钮。
- `create_action_request.py` 新增 `set-rule` 动作，玩家从前端或快速命令锁定规则时，先写入无正史影响的动作请求，再由建议命令调用 `set_world_rule.py`。
- `validate_world.py` 与 `smoke_test.py` 已纳入世界规则 schema、CANON 同步、交互包摘要、dashboard 摘要和前端标记检查。

### v0.75（2026-08-13）

- 新增 `scripts/check_world_rules.py`，用于在执行重要干预、随机覆盖或推进前创建最小规则冲突检查包。
- 规则检查包保存在当前枝丫 `runtime/rule-checks/<check_id>/`，属于 support 文件，不改变正史。
- 检查包只读取 `ACTIVE.md`、`setup/world-rules.json` 和可选 action request，列出相关 active 规则、来源指针和处理指令。
- 该脚本不假装进行复杂语义判决；当可能相关时输出 `needs-model-review`，由 AI 依据小包判断是否需要玩家 override、修改规则或创建分支。
- `init_world.py`、`create_branch.py` 与 `validate_world.py` 已纳入 `runtime/rule-checks` 目录和 `be-a-god.rule-check.v1` schema 检查。
- `create_action_request.py --payload-json` 的 Windows 解析路径已增强，JSON 字符串会优先按 JSON 解析，避免把含引号的 JSON 当作文件路径探测。

### v0.76（2026-08-13）

- 新增 `scripts/resume_world.py`，用于换新对话或读取 handoff 后生成最小恢复包。
- 恢复包保存在当前枝丫 `runtime/resume-packets/<resume_id>/`，包含 `resume.json` 与 `resume.md`，属于 support 文件，不改变正史。
- 恢复包会列出 `first_read`：`ACTIVE.md`、当前 `SAVE.md`、`PLAYER.md`、最新 `存档.md` 或 `CURRENT.md`、`setup/world-rules.json`、`dashboard/data.json`。
- 恢复包只摘要当前 save、玩家偏好、最新 handoff 指针、active 世界规则和 dashboard 计数，避免新对话启动时读取完整章节或平级分支。
- `init_world.py`、`create_branch.py` 与 `validate_world.py` 已纳入 `runtime/resume-packets` 目录和 `be-a-god.resume-packet.v1` schema 检查。
- `SKILL.md` 与主持协议已要求：新对话或 handoff 后先用 `resume_world.py --dry-run` 确定启动读取边界。

### v0.77（2026-08-13）

- 新增 `scripts/install_local_skill.py`，用于把项目内 skill 安装到 Codex 全局 skills 目录。
- 安装脚本默认只预览；必须显式 `--confirmed` 才会复制文件。
- 安装前自动调用 `check_install_ready.py`；目标目录已存在时拒绝覆盖，不删除、不重置、不进行破坏性操作。
- `SKILL.md` 新增简短安装说明：先 dry-run 预览，再由玩家确认是否复制到全局目录。
- `smoke_test.py` 将只覆盖安装脚本 dry-run，不执行真实全局安装。

### v0.78（2026-08-13）

- 前端模板新增“神之骰”面板，显示当前枝丫最近随机记录摘要与 `random-log.jsonl` 来源指针。
- `export_dashboard.py` 会从当前枝丫 `random/random-log.jsonl` 导出 `dashboard.random_log`，包含 count、latest 和 recent。
- `resolve_random.py` 在天气随机或玩家天气覆盖后，会同步更新 dashboard 的 weather、latest_random 和 compact random_log。
- 前端只展示随机摘要，不读取完整随机日志；玩家需要展开细节时仍通过来源指针回到当前枝丫随机日志。
- `smoke_test.py` 已检查随机摘要导出、天气覆盖后 dashboard 同步，以及前端 `random-log` / `renderRandomLog` 标记。

### v0.79（2026-08-13）

- `export_dashboard.py` 新增 `dashboard.attention` 摘要，导出 followed、ignored、plot-ready 的数量和紧凑列表。
- `export_dashboard.py` 会读取当前枝丫 `state/attention.json` 作为关注状态来源；`set_attention.py` 修改关注状态后会重建 dashboard 与 manifest。
- 前端模板新增“关注状态”面板，直接读取 `dashboard.attention`，不扫描完整实体卡。
- 被忽略角色仍保留棋子和状态，但常驻看板可以用轻量摘要提醒玩家，而不自动展开其个人剧情。
- `smoke_test.py` 已检查 ignored 状态写入后 dashboard attention 摘要更新，以及前端 `attention-list` / `renderAttention` 标记。

### v0.80（2026-08-13）

- `references/validation-scenarios.md` 从 5 个场景扩展到 10 个场景。
- 新增验收覆盖：世界规则锁定、规则冲突预检、新对话恢复、前端常驻面板和安装预览。
- 验收标准明确要求：规则不能只留在聊天记忆里，恢复不能依赖隐藏上下文，前端不能直接改正史，安装预览不能复制或覆盖全局文件。

### v0.81（2026-08-13）

- `build_timeline.py` 从“仅导出已确认事件”扩展为轻量综合时间线。
- 时间线现在包含 confirmed 事件节点、locked 活跃世界规则节点、queued/due 未来队列节点，以及当前枝丫下的 child branch 入口节点。
- 前端样例时间线新增 locked 与 queued 节点，CSS 新增 queued/due 时间线状态样式。
- `smoke_test.py` 已检查 locked rule、queued future 和 child branch 三类 timeline 节点。
- 修正 `SKILL.md` 中过期的 validation-scenarios 描述，不再写成“5 个 dry-run”。

### v0.82（2026-08-13）

- `create_world_brief.py` 新增 `Field source map`，明确标记 player-locked、polishable、player-setting、player-note 与 ai-fill 字段。
- `WORLD-BRIEF.template.md` 与前端创世弹窗生成器同步输出字段来源表。
- `validate_world_brief.py` 会在缺少字段来源表时给出 warning，避免创世信息来源只隐含在章节标题里。
- `smoke_test.py` 已检查创世草案包含字段来源表、玩家锁定字段和 AI-fill 字段来源。

### v0.83（2026-08-13）

- `build_timeline.py` 新增 ignored-character collapsed summary 节点。
- 当 `state/attention.json` 中存在 ignored 角色且该角色有关联事件时，timeline 会输出 `state: ignored` 的折叠摘要节点。
- 该节点只显示目标 ID、折叠事件数量和来源指针；具体故事仍需玩家主动调用 `build_ignored_digest.py` 查看。
- 前端样例时间线新增 ignored 节点，CSS 新增 `.timeline .ignored` 状态样式。
- `smoke_test.py` 已检查设置 ignored 后 timeline 会导出 ignored digest 节点。

### v0.84（2026-08-13）

- 同步 `agents/openai.yaml` 安装/UI 元数据，使它与当前 `SKILL.md` 能力范围一致。
- 安装界面描述现在明确包含本地 Markdown 世界、地图棋子、时间线、编年史、分支存档、随机日志和轻量上下文读取。
- 该变更只影响 Codex 安装与调用入口的展示说明，不改变存档结构、脚本行为或正史文件。

### v0.85（2026-08-13）

- `prepare_frontend.py` 现在会在世界本地前端目录生成 `README.md`。
- README 明确说明打开 `index.html`、三份 live JSON 的用途、sample JSON 的回退性质，以及前端只读正史边界。
- `smoke_test.py` 已检查准备出的前端目录包含 README，并包含只读边界、入口和三份数据文件标记。

### v0.86（2026-08-13）

- `validate_world.py` 新增 dashboard、timeline、map-layers 的结构级校验。
- dashboard 校验覆盖 schema、world_id、branch_id、time、focal_place、current_scene、pieces、pins、pending_action_requests、advance_profile、world_rules、random_log 和 attention。
- timeline 校验覆盖 schema、branch 归属、节点唯一 ID、节点状态，以及 event_id/time/label/source 指针。
- map-layers 校验覆盖 schema、levels、nodes、places 和只读 `read_policy`，避免前端地图误带完整故事正文。
- 同一套结构校验也会用于 `<world>/frontend` 中由 `prepare_frontend.py` 拷贝出的三份 live JSON。

### v0.87（2026-08-13）

- 修复 `init_world.py` 初始化时未把创世草案核心字段写入正式入口文件的问题。
- `world-spec.json` 现在保存 `creation_fields` 与 `creation_field_sources`，用于快速判断哪些字段来自玩家锁定、可润色、玩家设置、玩家备注或 AI 补全。
- `WORLD.md`、`PLAYER.md` 和初始 `SAVE.md` 会继承世界前提、起始时代、起始区域、神职、基调和绝对禁区等字段。
- 初始 dashboard、timeline、map-layers 也补齐 v0.86 所需结构，允许刚初始化后直接验证，不必先等待后续导出脚本覆盖。
- `smoke_test.py` 已检查神职、起点和字段来源表在初始化后没有丢失。

### v0.88（2026-08-13）

- `check_install_ready.py` 对 `agents/openai.yaml` 新增核心能力语义检查。
- 安装预检现在不仅检查 display_name、short_description、default_prompt 和隐式调用字段是否存在，也检查安装入口是否提到 Markdown、map pieces、timeline、chronicle、branch saves、random logs 和 lightweight context。
- 该检查用于防止后续功能扩展后，Codex 安装界面仍显示过期能力说明。

### v0.89（2026-08-13）

- 修复 `export_dashboard.py` 重建 dashboard 时会把天气重置为 `None` 的问题。
- dashboard 现在会从当前枝丫 `random/random-log.jsonl` 读取最近 weather 记录，恢复 `weather`，并输出 `latest_random`。
- 这样玩家手动覆盖天气后，即使后续因为关注状态、地图或其他操作重建 dashboard，常驻天气也不会丢失。
- `smoke_test.py` 已检查天气覆盖后直接重建 dashboard、以及修改 attention 触发 dashboard 重建时，天气仍保持为玩家覆盖值。

### v0.90（2026-08-13）

- 同步前端样例数据结构：`sample-dashboard.json` 新增 `schema: be-a-god.dashboard.v1`，`sample-timeline.json` 新增 schema、world_id 和 branch_id。
- `smoke_test.py` 已检查三份 sample JSON 的 schema，避免静态前端 fallback 数据落后于正式导出结构。

### v0.91（2026-08-13）

- `make_interaction_packet.py` 写入交互包后会刷新 `system/file-manifest.json`。
- 这保证交互包这类 support 文件进入文件清单，便于库存、溯源和安装前一致性检查；手动编辑检测仍按既有策略忽略 runtime 文件。
- dry-run 仍然只输出 JSON，不写入交互包，也不刷新 manifest。
- `smoke_test.py` 已检查 `story/main/runtime/interaction-packets/IP-SMOKE.json` 会出现在 manifest 中。

### v0.92（2026-08-13）

- 收紧 `build_ignored_digest.py --output` 的写入安全边界。
- 相对 output 现在按 world 根目录解析，最终输出必须留在 world 目录内。
- 默认拒绝覆盖已有 digest 输出，必须显式 `--overwrite` 才能替换。
- 写出 digest 后会刷新 `system/file-manifest.json`；不带 `--output` 时仍只把 digest JSON 打到 stdout。
- `smoke_test.py` 已检查 world-local ignored digest 输出和 manifest 登记。

### v0.93（2026-08-13）

- `SKILL.md` 新增“Inspect map, timeline, or frontend”工作流，直接路由地图层、时间线、dashboard、ignored digest 和 world-local 前端准备脚本。
- `SKILL.md` 新增“Random or player-overridden outcomes”工作流，明确随机天气、选择、整数和玩家 override 都应通过 `resolve_random.py` 记录。
- 手动 Markdown 编辑流程现在明确调用 `detect_manual_edits.py --world <world>` 生成 report 和 merge plan。
- `check_install_ready.py` 新增 direct player workflow script 路由检查，防止前端、地图、时间线、忽略摘要、随机和手动编辑脚本只存在于 catalog 而没有进入主 skill 工作流。

### v0.94（2026-08-13）

- `references/validation-scenarios.md` 从 10 个场景扩展到 15 个场景。
- 新增验收覆盖：创世字段初始化后不丢失、dashboard 重建保留天气、前端导出自带只读说明、ignored digest 输出安全、以及 fresh Codex 只读 `SKILL.md` 时仍能路由直触发工作流脚本。
- 这些场景对应 v0.85～v0.93 的新增能力，避免烟测通过但验收文档仍停留在旧 MVP 范围。

### v0.95（2026-08-13）

- `SKILL.md` 的“First step each turn”现在明确：当玩家可能直接编辑过文件时，先运行 `detect_manual_edits.py --world <world> --dry-run`。
- 这样手动编辑检测不只存在于后面的专题流程里，也进入每回合启动入口，降低直接继续游玩时漏检玩家改动的概率。

### v0.96（2026-08-13）

- `smoke_test.py` 新增失败路径验证工具 `run_expect_fail`。
- ignored digest 安全边界现在不只测试成功写入 world-local 输出，也测试：
  - `--output` 指向 world 外时必须失败；
  - 不带 `--overwrite` 覆盖已有 digest 时必须失败。
- 这补齐 v0.92 安全机制的负向验收，避免只靠正向烟测证明写入功能存在。

### v0.97（2026-08-13）

- `init_world.py` 初始 dashboard 的 `advance_profile.presets` 不再为空。
- 初始化时会把默认推进预设摘要写入 `dashboard/data.json`，使玩家刚创建世界后打开前端就能看到 step、hybrid、chronicle、event-watch 等推进方式。
- 初始 dashboard 也显式包含 `latest_random: null`，与后续 `export_dashboard.py` 的字段形态保持一致。
- `smoke_test.py` 已检查刚初始化后的 dashboard 包含默认推进预设摘要和 `latest_random` 字段。

### v0.98（2026-08-13）

- `check_install_ready.py` 将 direct player workflow script 路由缺失从 warning 提升为 error。
- 如果 `SKILL.md` 没有直接路由地图、时间线、dashboard、前端准备、ignored digest、随机结果或手动编辑检测脚本，安装预检会返回 not ready。
- 这样 `references/validation-scenarios.md` 的“Direct workflow routing”验收不再只是文档要求，而会被安装前检查强制执行。

### v0.99（2026-08-13）

- 修复 `init_world.py` 对 `WORLD-BRIEF.md` 多行 `Absolute prohibitions` 列表的解析。
- 玩家在创世草案中逐行填写的绝对禁区现在会以列表形式进入 `world-spec.json` 的 `creation_fields.absolute_prohibitions`，并以紧凑文本同步到 `WORLD.md`。
- `smoke_test.py` 已加入绝对禁区样例，检查初始化后不会丢失该玩家锁定内容。

### v1.00（2026-08-13）

- 修正 v0.99 的列表解析边界：`brief_list_after_field()` 现在会跳过字段行后的前置空行，再开始收集 bullet 列表。
- 该修复解决 `- Absolute prohibitions:` 后紧跟换行时，解析器提前停止、导致 smoke test 发现禁区列表为空的问题。

### v1.01（2026-08-13）

- `validate_world_brief.py --require-confirmed` 现在会把缺少必填创世字段视为 error，而不只是 warning。
- 必填确认字段包括 world premise、starting era、starting region 和 god role。
- 未确认草案仍可带缺字段 warning，方便玩家逐步填写；但 confirmed 草案不能缺少这些字段后进入 `init_world.py`。
- `smoke_test.py` 已加入确认态但缺少必填字段的反向测试。

### v1.02（2026-08-13）

- 补齐 `assets/world-template/WORLD-BRIEF.template.md`，使它与 `create_world_brief.py` 和前端创世弹窗的草案结构一致。
- 模板现在包含 `## Content boundary`、`- absolute bans:` 和 `## Player notes`，不再只写字段来源表和确认区。
- `smoke_test.py` 已检查 WORLD-BRIEF 模板包含内容边界、绝对禁区、玩家备注和字段来源表标记。

### v1.03（2026-08-13）

- `create_world_brief.py` 新增 `--content-ban` 参数，支持命令行生成创世草案时写入 `Content boundary` 下的 `absolute bans` 列表。
- 这使 Python 生成器与前端创世弹窗、WORLD-BRIEF 模板保持一致。
- `smoke_test.py` 已检查生成的 WORLD-BRIEF 同时包含内容边界、absolute bans 标记和玩家填写的内容禁区文本。

### v1.04（2026-08-13）

- 修复 WORLD-BRIEF 的内容边界在初始化时没有进入 `content-profile.json` 的问题。
- 当没有单独传入 `--content-profile` 时，`init_world.py` 会从 WORLD-BRIEF 的 `Content boundary` 读取 profile、notes 和 `absolute bans`。
- 玩家填写的内容禁区会写入 `setup/content-profile.json.player_absolute_bans`，同时进入 `world-spec.json.creation_fields.content_bans`。
- `smoke_test.py` 已检查内容禁区在 WORLD-BRIEF、world-spec 和 content-profile 三处都不会丢失。

### v1.05（2026-08-13）

- `validate_world_brief.py --require-confirmed` 现在要求 confirmed WORLD-BRIEF 必须包含 `Field source map`。
- 未确认草案缺少字段来源表仍然是 warning，允许玩家逐步填写；确认态缺少来源表则是 error。
- `smoke_test.py` 已加入确认态但缺少 Field source map 的反向测试。

### v1.06（2026-08-13）

- `init_world.py` 现在会把内容边界的紧凑摘要同步进 `PLAYER.md`，包括 `content_preset` 和 `content_absolute_bans`。
- 完整内容边界仍然保存在 `setup/content-profile.json`；交互包可优先读取 `PLAYER.md` 的摘要，减少无必要的上下文读取。
- `smoke_test.py` 已检查初始化后 `PLAYER.md` 不会丢失内容预设和玩家绝对禁区摘要。

### v1.07（2026-08-13）

- `set_content_profile.py` 更新玩家内容边界时，现在也会把具体绝对禁区文本同步进 `PLAYER.md`，不再只写禁区数量。
- `PLAYER.md` 的内容边界摘要与 `setup/content-profile.json` 保持一致，近景互动包可继续避免读取完整内容配置。
- `smoke_test.py` 已检查玩家后续修改内容边界后，`PLAYER.md` 仍包含 `content_preset` 和具体绝对禁区文本。

### v1.08（2026-08-13）

- `PLAYER.md` 初始化时改用可替换的 `Content profile summary` 和 `Advance profile summary` 摘要块，避免后续设置变更时残留旧摘要。
- `set_advance_profile.py` 更新推进偏好时，现在会把默认推进预设、模式、天数、漫游和队列暂停策略同步进 `PLAYER.md`。
- `smoke_test.py` 已检查初始化推进摘要、后续推进偏好同步，以及内容/推进摘要不会出现新旧值重复残留。

### v1.09（2026-08-13）

- `smoke_test.py` 现在直接覆盖 `validate_world_structure.py`，确认初始化后的世界目录满足最小结构要求。
- `smoke_test.py` 现在直接覆盖 `build_event_graph.py --dry-run` 和实际写入路径，确认初始事件会进入 `indexes/event-graph.json`。
- 这补齐了“通过事件图按因果、角色、地点和标签回溯旧事件”的基础验收面。

### v1.10（2026-08-13）

- `build_file_manifest.py --output` 现在会把相对路径解析到 world 内部，并拒绝写入 world 目录外的位置。
- 默认 manifest 输出仍是 `system/file-manifest.json`，保持原有机械刷新路径不变。
- `smoke_test.py` 已加入 world 外 manifest 输出必须失败的负向测试。

### v1.11（2026-08-13）

- `set_attention.py` 现在要求实际写入关注/忽略状态时必须带 `--confirmed`；`--dry-run` 仍可预览。
- 玩家点击“忽略/关注”后，Codex 可在明确玩家意图下传入 `--confirmed`，但脚本不再允许误调用直接改变状态。
- `smoke_test.py` 已检查未确认写入必须失败，确认后才能把角色设为 ignored。

### v1.12（2026-08-13）

- `SKILL.md` 的地图/时间线/前端流程现在明确说明：玩家明确要求关注或忽略目标时，调用 `set_attention.py ... --confirmed`。
- 若玩家意图不明确，先使用 `set_attention.py --dry-run` 预览，不直接改变关注状态。
- 这让主 skill 入口与 v1.11 的脚本确认闸门保持一致。

### v1.13（2026-08-13）

- `create_action_request.py` 为 ignore/follow 请求生成的建议命令现在会包含 `set_attention.py ... --confirmed`。
- 这修复了 v1.11 后 action request 建议命令与 `set_attention.py` 确认闸门不一致的问题。
- `smoke_test.py` 已检查 ignore action request 必须建议 confirmed attention update。

### v1.14（2026-08-13）

- `switch_branch.py` 现在要求实际切换 `ACTIVE.md` 时必须带 `--confirmed`；`--dry-run` 仍可预览目标分支。
- `SKILL.md` 的历史分支流程已同步为 `switch_branch.py --world <world> --branch-path <path> --confirmed`。
- `smoke_test.py` 已检查未确认切换必须失败，确认后才能切回父枝丫并重建派生前端数据。

### v1.15（2026-08-13）

- `create_action_request.py` 新增 `--preset`，advance-time 请求现在能把玩家选择或 dashboard 默认推进预设传给 `advance_world.py --preset <id>`。
- 前端模板的推进按钮建议命令已从只写 `intent: preset:<id>` 改为实际传入 `--preset <id>`。
- `smoke_test.py` 已检查 advance-time action request 保留所选推进预设，且前端模板包含 `--preset` 路由。

### v1.16（2026-08-13）

- `validate_world.py` 现在会检查 `PLAYER.md` 的 `Content profile summary` 与 `Advance profile summary`。
- 如果 `setup/content-profile.json` 的 preset/绝对禁区或 `setup/advance-profile.json` 的默认预设与 `PLAYER.md` 摘要不一致，验证会报错；缺少摘要块则给 warning，兼容旧存档。
- `smoke_test.py` 已加入篡改 `PLAYER.md` 摘要后的负向验证，确认 stale summary 会被发现。

### v1.17（2026-08-13）

- `check_install_ready.py` 的 direct workflow 检查扩展到 `create_action_request.py`、`set_attention.py`、`set_advance_profile.py`、`set_content_profile.py`、`set_world_rule.py` 和 `switch_branch.py`。
- 安装预检现在还会检查 `SKILL.md` 是否保留关键命令片段：关注/忽略确认、推进 preset、内容边界确认和分支切换确认。
- 这用于防止脚本能力已经存在，但新 Codex 只读 `SKILL.md` 时无法正确路由玩家直触发操作。

### v1.18（2026-08-13）

- `references/validation-scenarios.md` 从 15 项扩展到 18 项。
- 新增验收场景覆盖：`PLAYER.md` 紧凑偏好摘要一致性、关注/忽略与分支切换的确认闸门、以及前端/动作请求到 `advance_world.py --preset` 的推进预设链路。
- 这让 smoke 中新增的负向测试和文档验收面保持一致。

### v1.19（2026-08-13）

- `references/frontend-contract.md` 现在明确要求 advance-time 建议命令把 `dashboard.advance_profile.default_preset` 作为 `--preset <preset-id>` 传入。
- 不能只把 preset 写进 intent 文本，否则后端会退化为默认/单步推进。
- 该契约与 v1.15 的前端模板、`create_action_request.py` 和 smoke 覆盖保持一致。

### v1.20（2026-08-13）

- `check_install_ready.py` 新增轻量前端资产检查。
- 安装预检现在会检查 `sample-dashboard.json`、`sample-timeline.json`、`sample-map-layers.json` 的 schema，以及 `app.js` / `index.html` 中的关键路由片段。
- 这样即使不依赖完整 smoke，安装预检也能发现前端模板明显落后于当前 dashboard、timeline、map、action request 和 preset 契约。

### v1.21（2026-08-13）

- 修正蓝图中早期地图主题说明：第一版主题偏好只保存在前端本地展示层，不写入 `PLAYER.md`、dashboard 或 timeline。
- 修正“Skill 包结构草案”中的过期目录：当前使用 `assets/frontend-template/` 和 `assets/world-template/`，不再描述不存在的 `assets/dashboard/themes/`。
- 脚本树改为列核心入口与“详见 `references/script-catalog.md`”，避免蓝图长列表落后于实际 48 个脚本。

### v1.22（2026-08-13）

- `validate_world.py` 现在会按 action 类型检查动作请求的 `suggested_command` 是否路由正确。
- 覆盖范围包括：互动包 request-id、ignore/follow confirmed attention update、advance preset、天气 override、set-rule 和 branch draft。
- `smoke_test.py` 已加入篡改 ignore 请求建议命令后的负向验证，防止 stale action request 静默通过。

### v1.23（2026-08-13）

- `check_install_ready.py` 的 direct workflow 覆盖继续扩展到动作请求生命周期、规则冲突检查、推进/队列结算、handoff、恢复、分支草案/创建、分支切换和本地安装预览。
- 安装预检现在会强制确认 `SKILL.md` 保留这些核心入口的脚本路由和关键命令片段。
- 目的仍然是保证新 Codex 只读取 `SKILL.md` 时，不会错过玩家直触发流程。

### v1.24（2026-08-13）

- 修正 `references/storage-contract.md` 中恢复流程的表述：新对话或 handoff 后应先用 `resume_world.py --dry-run` 获取最小 `first_read` 读取边界。
- 只有需要把恢复包持久写入 `runtime/resume-packets/` 时，才使用 `resume_world.py --confirmed`。
- 该修正让存储契约与 `SKILL.md`、主持协议和脚本实际确认闸门保持一致，避免继续游戏时误把支持性恢复包写入当作必要步骤。

### v1.25（2026-08-13）

- 修正神力代价评估的文档边界：玩家仍在考虑行动时，`assess_divine_action.py` 应使用 `--dry-run` 预览，不写入文件。
- 只有需要把 support-only 的代价评估包保存到 `runtime/divine-assessments/` 时，才使用 `--confirmed`。
- 该评估包仍然没有正史影响；真正改变世界必须继续走实际行动或结算脚本。

### v1.26（2026-08-13）

- `SKILL.md` 的推进时间流程现在明确：记录已知未来触发器、旅行抵达、警告或高优先级暂停事件时，应调用 `queue_event.py --world <world> ... --confirmed`。
- `game-master-protocol.md` 与 `script-catalog.md` 已同步 `queue_event.py` 的确认闸门：不确定时用 `--dry-run`，写入事件队列时必须确认。
- `check_install_ready.py` 已把 `queue_event.py` 和关键命令片段纳入 direct workflow 检查，避免新 Codex 忘记队列写入确认要求。

### v1.27（2026-08-13）

- `smoke_test.py` 新增 `queue_event.py` 负向测试：不带 `--confirmed` 的队列写入必须失败。
- `references/validation-scenarios.md` 从 18 项扩展到 19 项，新增“Future event queue confirmation”验收场景。
- 该场景防止模糊未来触发器或误调用脚本静默写入队列，随后错误地暂停世界时间。

### v1.28（2026-08-13）

- `check_install_ready.py` 新增 validation-scenarios 覆盖检查。
- 安装预检现在会确认验收场景编号至少覆盖到 19，并包含 direct workflow、advance preset 和 future event queue confirmation 等关键场景。
- `script-catalog.md` 已同步说明该预检会检查验收场景覆盖，避免验收文档在脚本能力扩展后落后。

### v1.29（2026-08-13）

- `prepare_frontend.py --output` 现在会拒绝 world 目录外的输出路径，确保静态前端始终是 world-local artifact。
- `smoke_test.py` 新增前端外部输出负向测试：即使是 `--dry-run`，world 外输出也必须失败。
- `script-catalog.md` 与验收场景 13 已同步该安全边界，避免前端导出散落到世界目录之外。

### v1.30（2026-08-13）

- 前端详情面板中的动作请求、规则和随机记录 JSON 预览现在会先经过 `escapeHtml(JSON.stringify(...))` 再插入 HTML。
- `references/frontend-contract.md` 新增要求：dashboard、timeline、map、action request、rule 和 random log 文本即使来自本地文件，也按不可信展示数据处理。
- `check_install_ready.py` 已把前端 JSON 预览转义标记纳入轻量前端资产检查。

### v1.31（2026-08-13）

- `check_install_ready.py` 的 validation-scenarios 检查现在要求验收场景编号连续。
- 如果未来文档跳号、漏号或只保留最大编号，安装预检会直接报错并输出实际编号列表。
- 该规则用于防止验收文件在持续扩展时产生静默缺口。

### v1.32（2026-08-13）

- 修正 `references/frontend-contract.md` 的地图点击语义：点击棋子或图钉只负责选择目标并展示紧凑详情/source pointer。
- 真正生成 action request 的是后续神力动作按钮；Codex 只有在玩家继续执行时才创建 interaction packet。
- 这与当前前端模板行为一致，也更符合“点开先看故事，必要时再触发剧情”的交互边界。

### v1.33（2026-08-13）

- `SKILL.md` 的主循环也同步了点击语义：map click 默认是 selection/inspection，不再描述为自动创建 action request。
- 当前 action request 的正式入口是 frontend button 或 quick player command。
- 这样新 Codex 读取主 skill 时，不会把普通查看误判为需要写入 `runtime/action-requests/`。

### v1.34（2026-08-13）

- `game-master-protocol.md` 已同步：普通 map click 只代表选择或查看；只有前端动作按钮或明确 quick player command 才写入 action request。
- 蓝图正文中的前端点击说明和 `runtime/action-requests/` 目录注释也已同步。
- 这进一步避免“玩家只是点开看看”被误解释为一次需要持久记录的行动。

### v1.35（2026-08-13）

- 前端模板补齐“创建分支”按钮 `branch-action`，与 frontend contract 中 god action panel 的 branch 能力一致。
- `app.js` 现在会为该按钮生成 `create_action_request.py --action branch` 建议命令，并携带 `fork_event` 与 `change_summary` payload。
- `check_install_ready.py` 与 `smoke_test.py` 已纳入 branch 前端入口和路由 marker 检查，避免按钮或脚本路由回退。

### v1.36（2026-08-13）

- `smoke_test.py` 新增 `create_action_request.py --action branch` 后端路由检查。
- branch 动作请求必须建议 `draft_branch.py ... --confirmed`，不能直接改写父分支历史。
- `references/validation-scenarios.md` 从 19 项扩展到 20 项，新增“Branch action request routing”；安装预检的最低验收编号也同步提升到 20。

### v1.37（2026-08-13）

- 修复 branch action request 的真实参数错误：`draft_branch.py` 的实际参数是 `--fork-event`，不是 `--from-event`。
- `create_action_request.py` 现在生成 `draft_branch.py --fork-event ... --confirmed`；同时兼容旧 payload 中的 `from_event`，但前端已改为输出 `fork_event`。
- `validate_world.py`、`smoke_test.py`、`SKILL.md`、验收场景和安装预检都已同步检查 `--fork-event`，防止错误参数再次通过。

### v1.38（2026-08-13）

- 继续修复 branch action request 参数完整性：`draft_branch.py` 还要求 `--branch-id`。
- `create_action_request.py` 现在会从 payload 读取 `branch_id`，缺失时在建议命令中保留 `<branch-id>` 占位；前端 branch 按钮也会输出该字段占位。
- `validate_world.py`、`smoke_test.py`、`SKILL.md`、验收场景和安装预检都已同步检查 `--branch-id`，确保建议命令能真正进入分支草案脚本。

### v1.39（2026-08-13）

- `SKILL.md` 中地图/棋子流程现在显式展示 `move_entity.py --world <world> --entity-id <id> ... --confirmed` 和 `wander_entities.py --world <world> --confirmed`。
- `check_install_ready.py` 的 direct workflow 检查新增 `create_entity.py`、`move_entity.py` 和 `wander_entities.py`，并检查对应确认命令片段。
- 验收场景 15 已同步包含 entity creation、piece movement 和 wandering，避免这些玩家直触发能力只存在于专题文档。

### v1.40（2026-08-13）

- `smoke_test.py` 为 branch action request 增加负向验证。
- 当请求的 `suggested_command` 丢失 `--branch-id` 时，`validate_world.py` 必须报错，不能让不可执行的分支草案命令静默通过。
- 该测试与 v1.38 的参数完整性修复形成回归保护。

### v1.41（2026-08-13）

- 前端 `branch-action` 现在会先弹出最小填空，要求玩家填写 `branch_id` 和 `change_summary`，再生成 branch action request。
- `fork_event` 使用当前选中棋子/图钉/时间线节点的事件或 ID；缺少必要输入时不生成请求。
- 前端新增 `commandJsonArg()`，对命令中的 JSON 参数做转义，避免分支摘要里的引号破坏建议命令；前端契约、smoke 和安装预检已同步该要求。

### v1.42（2026-08-13）

- `commandJsonArg()` 进一步补齐反斜杠转义。
- 分支摘要或枝丫 ID 中若包含路径符号、反斜杠或引号，前端生成的 `--payload-json` 建议命令仍应保持 JSON 字符串结构。
- 这属于前端命令展示层安全，不改变任何正史或脚本写入行为。

### v1.43（2026-08-13）

- `draft_branch.py` 与 `create_branch.py` 不再把无法 slug 化的 `branch_id` 静默兜底为 `branch`。
- 如果玩家输入的分支 ID 规范化后为空，脚本会报错并要求重新提供包含字母、数字、下划线或连字符的 ID。
- `smoke_test.py` 新增非法 branch-id 负向测试；`storage-contract.md` 已同步该规则。

### v1.44（2026-08-13）

- `create_entity.py` 现在会拒绝不安全的 `--entity-id` 和 `--slug`，只允许字母、数字、下划线和连字符。
- 这避免玩家或前端输入把实体卡写成异常文件名或尝试路径逃逸。
- `smoke_test.py` 新增非法 entity-id / slug 负向测试；`script-catalog.md` 和验收场景 15 已同步该安全边界。

### v1.45（2026-08-13）

- `move_entity.py` 的实体解析收紧：先按卡片 `- id:` 精确匹配，其次只允许唯一文件名前缀匹配。
- 不再因为某张实体卡正文里提到目标 ID 就把它当成目标，避免棋子移动误伤文本引用对象。
- `smoke_test.py` 新增 decoy 实体测试；`script-catalog.md` 已同步新的解析边界。

### v1.46（2026-08-13）

- `validate_world.py` 新增 active branch state card 校验。
- 校验范围包括实体/地点卡必须有 `- id:`、ID 只能包含安全文件名字符、文件名必须以 ID 开头、同一 active branch 内 ID 不能重复。
- `smoke_test.py` 新增篡改 state card unsafe ID 的负向测试；`script-catalog.md` 已同步 validate_world 的覆盖范围。

### v1.47（2026-08-13）

- `make_interaction_packet.py` 的目标解析收紧：先按状态卡 `- id:` 精确匹配，其次只允许唯一文件名前缀匹配。
- 交互包不再用正文包含的目标 ID 做匹配，避免玩家点击 A 时误读“只是提到 A”的 B 卡片。
- 显式 `--target-file` 也必须位于当前世界目录内；`smoke_test.py` 和验收场景 21 已加入对应回归保护。

### v1.48（2026-08-13）

- `build_ignored_digest.py` 的事件归属收紧：只按事件元数据 `actors` 或旧版 `target_id` 判断目标是否参与。
- 不再因为事件正文里提到被忽略角色 ID 就把该事件收进摘要，避免“传闻/旁白提及”污染被忽略角色时间线。
- `smoke_test.py` 新增 decoy event 测试；`script-catalog.md` 和验收场景 14 已同步该边界。

### v1.49（2026-08-13）

- `build_timeline.py` 的 ignored collapsed node 也改为使用同一套事件归属规则：`actors` 或旧版 `target_id`。
- 时间线不再把正文提到被忽略角色 ID 的事件计入折叠节点，避免前端提示“某人有新动态”时被旁白或传闻误触发。
- `smoke_test.py` 的 decoy event 现在同时覆盖 timeline 和 ignored digest 两条路径。

### v1.50（2026-08-13）

- 为所有会进入路径的 ID 增加安全字符校验：request、packet、branch draft、handoff、resume、rule-check、divine-assessment、soften-request、manual-edit report、checkpoint。
- 这些 ID 现在只允许字母、数字、下划线和连字符；包含 `../`、斜杠、空格等路径字符时会失败。
- `smoke_test.py` 新增对应负向测试；`storage-contract.md`、`script-catalog.md` 和验收场景 22（Path-backed id safety）已同步该横切规则。

### v1.51（2026-08-13）

- `settle_interaction.py --packet <packet-id>` 现在按当前 `ACTIVE.md` 指向的枝丫查找交互包，不再固定从 `story/main/runtime/interaction-packets/` 读取。
- 显式 packet 路径仍必须位于世界目录内；packet ID fallback 仍受安全字符校验。
- 这保证子枝丫内的快速交互可以直接用 packet ID 结算，不需要手动传完整路径。

### v1.52（2026-08-13）

- `validate_world.py` 新增 `ACTIVE.md` 指针边界检查。
- `branch_path` 和 `save_path` 必须是世界内相对路径，不能是绝对路径或 `../` 逃逸；`save_path` 还必须指向当前 active branch 的 `SAVE.md`。
- `smoke_test.py`、`storage-contract.md` 和验收场景 23 已同步该规则。

### v1.53（2026-08-13）

- `check_install_ready.py` 新增 path-backed ID safety 预检项。
- 安装前会检查关键脚本是否保留 `SAFE_ID_PATTERN` / `validate_id` 以及对应参数标记，并检查 `smoke_test.py` 是否仍覆盖各类 unsafe ID 失败路径。
- 这让路径型 ID 安全不只依赖人工记忆或单次 smoke，而成为安装前的固定 Gate。

### v1.54（2026-08-13）

- `validate_world.py` 新增 manifest 路径边界检查。
- `system/file-manifest.json` 中的每个 `path` 必须先证明是世界内相对路径，才能继续做存在性检查。
- `smoke_test.py` 和验收场景 24 已覆盖 manifest `../` 逃逸失败路径。

### v1.55（2026-08-13）

- `create_branch.py` 新增子枝丫 state 快照继承：创建子枝时复制父枝丫当前 `state/entities/`、`state/locations/` 和直接 `state/*` 文件。
- 子枝丫不复制父枝丫 `events/`、`chronicle/`、`queues/`、`random/random-log.jsonl`、`runtime/`、`checkpoints/` 或嵌套 `branches/`，避免把父历史和后续随机流带入新分支。
- `smoke_test.py` 已覆盖子枝丫继承人物/地点/地图棋子，以及子枝丫后续互动状态不会回写父枝丫。
- `SKILL.md`、`storage-contract.md`、`script-catalog.md`、验收场景 25 和安装预检已同步该规则。

### v1.56（2026-08-13）

- 静态前端默认纸质地图主题改为羊皮纸与漫画墨线质感：浅色纸面、噪点纹理、手绘边框、漫画式阴影、抽象徽章棋子和纸质事件图钉。
- 神性星盘主题保留为暗色模式；主题切换仍只改变前端展示，不改变地图坐标、地点 ID、事件指针或任何世界文件。
- `frontend-contract.md`、`smoke_test.py` 和 `check_install_ready.py` 已加入 `paper-map`、`parchment-texture`、`ink-outline`、`comic-shadow` 等样式标记检查。

### v1.57（2026-08-13）

- 静态前端地图继续增强为更明确的手绘地图面板：新增展示层地形装饰、河流/支流、山丘、森林、地图标题卷轴、罗盘、比例尺和图例。
- 这些元素全部由前端展示层生成，不进入 dashboard、timeline、map-layers 或任何世界正史文件。
- 星盘主题下保留同一套数据点位，装饰层会弱化为星图式线条，不改变坐标和源指针。
- `frontend-contract.md`、`smoke_test.py` 与 `check_install_ready.py` 已加入 `renderMapDecorations`、`terrain-layer`、`compass-rose`、`scale-bar`、`map-legend` 等检查。

### v1.58（2026-08-13）

- 前端棋子新增状态环与图例扩展：ordinary、wandering/moving、plot-ready、followed、ignored、paused/danger、dead/hidden 具备不同视觉提示。
- `app.js` 新增 `classToken()`，把本地 JSON 中的 state/status/level 字段规范化后再作为 CSS 类名使用，避免异常状态字符串污染 DOM class。
- `frontend-contract.md`、`smoke_test.py` 与 `check_install_ready.py` 已把状态环和类名规范化纳入检查。

### v1.59（2026-08-13）

- 地形装饰升级为可变 SVG brush 数据链：`base/maps/terrain-brushes.json` 保存初始底图笔触，当前枝丫 `state/terrain-brushes.json` 保存神谕覆盖/新增/删除，`build_map_layers.py` 合并后导出到 `dashboard/map-layers.json.brushes`，前端用 `renderBrushSvg()` 渲染为粒子画笔。
- 新增 `scripts/set_map_brush.py`，在玩家确认后向当前 active branch 创建、更新或删除 terrain brush，并刷新地图层与 manifest；`--dry-run` 可预览，写入必须 `--confirmed`。
- `validate_world.py` 会校验 brush 类型、点位范围、非负宽度/密度/抖动和 source 指针；`smoke_test.py` 已覆盖 unsafe brush-id、神谕创建支流和 dashboard brush 更新。
- 固定 CSS 河流/森林装饰已替换为数据驱动 SVG brush，避免地形只存在于前端样式而无法被神谕修改；子枝丫会通过 state 快照继承父枝丫 fork 时的地形 brush 覆盖层。

### v1.60（2026-08-13）

- 按玩家选择，地形编辑路线确定为“两者都做，但先做命令式，后做画笔编辑器”。
- 命令式链路新增 `terrain-brush` action：玩家自然语言下达“移动河流 / 添加支流 / 抬升山丘 / 扩张森林”等神谕时，前端或 Codex 先创建非正史行动请求，不直接写地图。
- `create_action_request.py --action terrain-brush` 会保存 `brush_id`、`kind`、`change_summary`、`points_json` 或几何占位符，并建议确认后的 `set_map_brush.py` 命令。
- 前端新增“改地形”按钮，作为全局神谕入口；它只输出行动请求 payload 和建议命令，不直接改变 `dashboard/map-layers.json`。
- 后续画笔编辑器必须复用同一套 `terrain-brushes.json` 数据层，不能另建一套不可回溯的地图格式。

### v1.61（2026-08-13）

- 前端新增轻量画笔编辑器：玩家可输入 brush ID、选择 river/tributary/hills/forest/custom，在纸质地图上点选，生成 0..100 坐标的 `points-json`。
- 地图点选只生成临时 SVG 预览层 `brush-editor-preview`，不会直接写入 dashboard、timeline、map-layers 或世界正史文件。
- “生成地形神谕”按钮会输出同一套 `create_action_request.py --action terrain-brush` 建议命令；后续仍由 `set_map_brush.py --confirmed` 正式写入当前枝丫。
- 这样命令式和画笔式共用同一个 terrain brush 存储层：命令式适合自然语言快速改地形，画笔式适合玩家手工指定关键点位。

### v1.62（2026-08-13）

- 使用方式扩展为双 host：默认可直接在 Codex 对话里推进剧情；也可以接入玩家自己的大模型 API，在 Codex 外部运行语义生成。
- 新增 `setup/llm-api.config.json` 世界级可见配置，初始化世界时自动生成；模板位于 `assets/world-template/llm-api.config.template.json`。
- 配置采用 OpenAI-compatible chat/completions 协议，用户可改 `base_url`、`endpoint_path`、`model`、`api_key_env`、`temperature`、`max_tokens` 等字段；默认不写真实 key。
- 新增 `scripts/call_llm.py` 作为通用适配脚本：默认 dry-run 只预览请求，不联网；必须显式 `--call` 才调用外部 API。
- 外部模型只负责语义生成和结果建议，不能直接改正史；所有持久写入仍必须通过 `settle_interaction.py`、`settle_queued_event.py` 或其他确认脚本完成。

### v1.63（2026-08-13）

- 增加玩家快速开始说明 `references/player-quickstart.md`，覆盖安装、创建世界、继续游玩、打开前端、地形编辑、换新对话续档、外部 API 配置。
- 前端推进为双模式：静态模式仍可直接打开 HTML；本地 App 模式可用 `scripts/serve_frontend.py --world <world>` 启动 localhost。
- 本地 App API 只提供 `/api/health`、`/api/state`、`/api/action-request`：可刷新看板/地图/时间线，可创建非正史行动请求，但不能直接结算正史。
- `prepare_frontend.py` 生成的前端 README 已说明静态模式和本地 App 模式，以及“行动请求不是正史写入”的边界。
- 安装前 Gate 更新为 30 个验收场景，覆盖外部模型 API 和本地前端 App API 两条新边界。

### v1.64（2026-08-13）

- 玩家选择默认叙事方向为“混合模式，但偏真实历史感”：近景角色有戏，长期文明自然演化。
- 新增 `assets/world-template/narrative-profile.template.json` 和新世界内 `setup/narrative-profile.json`，默认 profile 为 `hybrid-historical`。
- 叙事优先级固定为 causality > world_continuity > character_agency > historical_texture > dramatic_presentation，避免 AI 为了爽点破坏因果和正史连续性。
- 重大事件必须能拆成 `visible_narration`、`gm_summary`、`settlement_plan` 三层；没有落盘计划的华丽文本不能直接视为正史。
- 事件生成新增压力链：pressure -> actor_choice -> direct_consequence -> second_order_consequence -> player_intervention_point；压力来源至少覆盖人物、资源、制度、环境。
- 重要角色默认要有 desire、fear、misunderstanding、resources、relationships、secret、god_view 等语义字段；脚本只负责存储/索引/结算，性格和动机仍交给大模型或玩家。
- `validate_world.py`、`smoke_test.py`、`check_install_ready.py` 和验收场景 31 已把叙事配置纳入固定 Gate；Codex 内游玩和外部 API host 使用同一套叙事边界。

### v1.65（2026-08-13）

- 叙事配置进一步进入运行时上下文：`make_interaction_packet.py` 会在 close-up 交互包内附带 compact `narrative_profile`。
- 交互包的 allowed sources 会明确包含 `setup/narrative-profile.json` 指针，但仍不读取章节全文或平级分支，符合“先快速交互，必要时再精确回溯”的 token 预算原则。
- `call_llm.py` 在 dry-run 和真实 call 前都会把同一套 compact narrative profile 注入 prompt，确保外部 API 模式也遵守 `hybrid-historical` 默认叙事边界。
- `smoke_test.py` 已覆盖交互包和外部模型请求内必须出现 `hybrid-historical` 与 `settlement_plan`，避免叙事规则只停留在初始化文件和前端展示里。

### v1.66（2026-08-13）

- 新增 `scripts/validate_settlement_result.py`，作为 Codex 或外部 API 生成结算 JSON 后、写入正史前的质量 Gate。
- 该 Gate 要求结果至少有玩家可见文本/summary、GM 摘要，以及 explicit `settlement_plan` 或旧式可结算字段（event、chronicle、consequences、state_appends、save_updates、dashboard）。
- `settle_interaction.py` 与 `settle_queued_event.py` 在写正史前都会调用同一校验逻辑，拒绝只有漂亮段落、没有任何可落盘后果的结果。
- 已确认写入的事件文件会保存 `## GM summary` 和 `## Settlement plan`，方便后续通过事件图和 source pointer 精准回溯，不需要读取长篇剧情。
- 验收场景扩展到 32 个，新增“Settlement result quality gate”。
