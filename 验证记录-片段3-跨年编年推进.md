# 验证片段 3：跨月或跨年编年史推进

- 日期：2026-08-12
- 当前世界/分支：无正式世界；本次为设计干跑验证
- 玩家输入：

> 让世界自然运行一年，只在战争、死亡、政权更替、关键背叛或需要我决定时暂停；默认用客观简史展示。

## 本轮允许读取

- 当前分支 `ACTIVE.md` 与 `SAVE.md`。
- 当前推进预设与玩家临时停止条件。
- 当前分支事件队列、活跃实体状态、随机种子和随机日志位置。
- 当前分支客观简史和时间线导出状态。

不允许读取：

- 全部章节正文。
- 平级分支。
- 背景角色的完整个人剧情。
- 未发生未来事件的正文。

## 设计干跑输出

### 推进请求应生成运行记录

```yaml
advance_run_id: ADV-000001
branch_id: BR-MAIN
requested_span: 1-year
start_time: <当前世界时间>
stop_conditions: [war, death, succession, betrayal, player-decision]
display_mode: objective-chronicle
focus_layer: scene-by-scene
active_layer: daily-or-event
background_layer: monthly
```

该运行记录应进入当前分支 `runtime/advance-runs/`，用于记录推进游标、已结算时间片、随机调用和暂停原因。

### 一年推进必须拆成时间片

- 焦点层：逐场景或逐关键事件。
- 活跃层：按天、旅程节点或计划节点。
- 背景层：按月或季度，只记录事件骨架。
- 每个时间片结算后更新推进游标。
- 到达停止条件时立即停止，不继续预写剩余年份。

### 自动暂停应生成暂停包

暂停包至少包含：

- 暂停原因。
- 发生时间和地点。
- 涉及角色、势力、地点和事件 ID。
- 已经写入正史的事实。
- 尚未结算的选择。
- 玩家可选行动。
- 继续推进时应从哪个推进游标恢复。

## 通过情况

结果：部分通过。

已通过：

- 蓝图已有混合推进、精度分层和自动暂停项。
- 蓝图已有 `advance_world.py`、随机日志、客观简史和时间线。
- 蓝图已要求背景事件不扩写成长篇场景。
- 蓝图已规定时间线只包含当前分支血统链。

发现的问题：

1. 没有明确跨期推进运行记录的位置，无法审计“一年推进”中途处理到了哪里。
2. 自动暂停缺少标准暂停包格式。
3. 没有明确到达暂停条件后禁止继续预写剩余未来。
4. 背景层事件骨架与客观简史、事件队列之间的写入边界还需要更明确。

## 建议修订

- 在每个分支 `runtime/` 下增加 `advance-runs/`。
- 明确 `advance_world.py` 必须分片推进并写入推进游标。
- 增加暂停包字段，暂停后只允许写入已发生事实和未决选择，不写剩余未来。
- 明确背景层普通变化只进客观简史骨架或队列，不生成完整章节。

## 是否继续片段 4

可以。片段 3 的问题是批量推进的审计和暂停边界缺口，补回后可以继续验证历史分支。
