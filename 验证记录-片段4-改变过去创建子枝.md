# 验证片段 4：改变过去并创建子枝

- 日期：2026-08-12
- 当前世界/分支：无正式世界；本次为设计干跑验证
- 玩家输入：

> 回到“王子在春潮祭前夜失踪”这个事件之前，让我以神谕阻止他离开王宫。

## 本轮允许读取

- 当前分支 `ACTIVE.md` 与 `SAVE.md`。
- 目标历史事件节点：王子失踪事件。
- 目标事件之前的父分支状态视图。
- 与目标事件直接相关的人物、地点、势力和锁定规则。
- 父分支截至 `fork_event` 的随机位置和地图版本。

不允许读取：

- 父分支在分叉点之后的后续事件，除非只用于向玩家说明“你正在从这里另开分支”。
- 平级分支内容。
- 当前分支之外的随机日志。
- 未被目标事件指针引用的大段历史正文。

## 设计干跑输出

### 创建子枝前应生成分支草案

在正式创建子枝前，系统应生成一份分支草案：

```yaml
branch_draft_id: BD-000001
parent_branch_id: BR-MAIN
fork_event: EVT-PRINCE-DISAPPEARS
inherit_until: EVT-BEFORE-SPRING-TIDE-EVE
parent_head_at_fork: EVT-PRINCE-DISAPPEARS
proposed_branch_name: 春潮祭前夜的阻止
player_intent: 以神谕阻止王子离开王宫
requires_confirmation: true
```

草案应写入父分支 `runtime/branch-drafts/`，等待玩家确认。确认前不得修改 `ACTIVE.md`，不得创建正式子枝。

### 玩家确认后应创建子枝

`create_branch.py` 应：

- 创建子枝目录与 `SAVE.md`。
- 写入 `parent_save`、`parent_branch_id`、`fork_event`、`inherit_until`、`parent_head_at_fork`。
- 从父分支截至 `inherit_until` 的视图生成子枝初始状态。
- 派生子枝独立随机流。
- 重建当前子枝索引、地图、时间线和前端数据。
- 根据玩家选择决定是否立刻切换 `ACTIVE.md` 到新子枝。

### 子枝第一条事件

改变过去不是只创建空分支。子枝中必须生成第一条神谕事件：

```yaml
event_type: divine-revision
branch_id: <child-branch-id>
world_time: <fork_event 之前或同一时间点>
source_parent_event: EVT-PRINCE-DISAPPEARS
player_intent: 阻止王子离开王宫
canon: true
```

该事件只属于子枝，不回写父分支。

## 通过情况

结果：部分通过。

已通过：

- 蓝图已明确改变过去默认创建子枝，不覆盖父分支。
- 蓝图已定义 `SAVE.md` 的父指针、分叉事件、继承边界和写时分离。
- 蓝图已规定平级分支不可见，随机流独立。
- 蓝图已规定时间线只合成父级继承范围和当前枝事件。

发现的问题：

1. 没有明确创建子枝前的分支草案和玩家确认位置。
2. 没有明确子枝创建后第一条“改变过去的神谕事件”必须写入子枝。
3. 没有明确创建子枝后是否自动切换 `ACTIVE.md`，需要由玩家确认或默认规则决定。
4. 随机流派生原则已有，但子枝 `random/seed.json` 的派生输入需要在分支创建规则中再次钉死。

## 建议修订

- 在每个分支 `runtime/` 下增加 `branch-drafts/`。
- 明确 `create_branch.py` 先生成草案，玩家确认后才创建正式子枝。
- 明确子枝第一条事件是 `divine-revision`，只写入子枝。
- 明确默认创建后切换到新子枝，除非玩家选择“只创建不切换”。
- 在分支创建流程中明确子枝随机种子由根种子、父分支 ID、`fork_event` 和新分支 ID 派生。

## 是否继续片段 5

可以。片段 4 的问题集中在分支创建前后的确认和首事件落点，补回后可继续验证手动修改 Markdown 后合并。
