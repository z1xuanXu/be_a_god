# 模拟神 Skill 游戏设定蓝图 v1.70 补充：可缩放纸质地图与中世纪 PNG 素材

## 本轮确定

- 前端地图不再只做“世界 / 地区 / 场景”层级切换，还必须支持真实视觉缩放、滚轮缩放、按钮缩放、重置视角、拖拽平移。
- 缩放和平移只改变玩家视角，不改变世界坐标、剧情文件、dashboard、map-layers 或任何 canon 状态。
- 画笔编辑器在地图缩放/平移后仍然输出稳定的 `0..100` 地图坐标，避免玩家改河流、支流、山丘、森林时坐标漂移。
- 地图视觉升级为“纸质地图 + 有点漫画质感 + 中世纪手绘素材堆砌”的方向。

## 新增前端素材集

素材保存在：

```text
be-a-god/assets/frontend-template/img/
```

当前最小素材集：

- `flag-marker.png`：棋子和事件图钉的小旗装饰。
- `forest-stamp.png`：森林画笔的手绘树木印章。
- `hills-stamp.png`：山丘/山地画笔的手绘地形印章。
- `creek-stamp.png`：河流/支流画笔的小溪水带印章。
- `parchment-overlay.png`：纸纹、污渍、墨点叠层。
- `corner-ornament.png`：地图角落装饰。

这些素材是显示层，不是世界真相。世界真相仍然来自 `map-layers.json` 和 active branch 的 `state/terrain-brushes.json`。

## 前端行为

- `map-content` 是可变换地图内容层，棋子、地点、事件图钉、SVG 地形和 PNG 地形印章都挂在这个层里。
- 标题、指南针、比例尺、图例和角落装饰保持在 UI 层，不随地图内容一起缩放。
- 非画笔模式：左键拖拽地图平移。
- 画笔点选模式：拖拽禁用，点击地图记录地形点。
- 鼠标滚轮或 `＋ / − / 重置` 控件改变视角。

## 验收边界

- `prepare_frontend.py` 必须把 `img/` 一起复制进 `<world>/frontend/img/`。
- 安装就绪检查必须验证 PNG 文件存在且文件头有效。
- smoke test 必须验证模板前端和导出的世界前端都包含缩放标记、素材标记和 `img/` 文件。
- 不允许素材替代地形数据源；PNG 只能帮助玩家更直观看见森林、山丘、河流、小溪。
